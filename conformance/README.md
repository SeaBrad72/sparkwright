# Conformance Checks

A **conformance check** proves that a reference implementation still satisfies its binding **contract** (see `../MAINTAINING.md` §1). Checks are how the kit — and every project that adopts it — enforces the contracts instead of merely describing them.

## Two kinds of check

- **Checklist** — a human/agent-completed list with explicit evidence per item. Used when judgment is required (e.g. architecture conformance). Gates at a human checkpoint.
- **Script** — an automated assertion runnable in CI. Used when the check is mechanical (e.g. "the CI pipeline runs a secret-scan step"). Gates in the pipeline.

## Where checks run

- **In the kit's own CI** (`.github/workflows/ci.yml`) — the kit proves it satisfies its own contracts.
- **In an adopting project** — at the gate named by the contract (Review, Definition of Done, etc., per `../DEVELOPMENT-PROCESS.md` §7).

## What a green run means — and doesn't

Conformance checks fall into two honesty classes. Run **`sh conformance/verify.sh`** for an aggregate that labels each one:

- **control** — proves a *working* control holds: the agent guard denies the destructive battery (`agent-autonomy.sh`), CI declares the required gate ids (`ci-gates.sh`), the guard is wired (`guard-wired.sh`), `main` is protected on the remote (`branch-protection.sh`), links resolve (`check-links.sh`), named backlog backends agree (`backlog-adapters.sh`), and — conditionally — the image supply-chain is wired (`container-supply-chain.sh`, only when a Dockerfile is present, so it is not in `verify.sh`'s unconditional aggregate). Green here is load-bearing.
- **documentation / evidence** — proves a procedure is *written down* and (for readiness) a drill **date is recorded** — NOT that the rollback, restore, or fault-injection was actually tested: `deployable-ready.sh`, `dr-ready.sh`, `resilience-ready.sh`, `observability-ready.sh`, `eval-ready.sh`, `responsible-ai-ready.sh`, `test-data-ready.sh`, `preview-env-ready.sh`, `agentops-ready.sh`, and the paired `*-readiness.md` / `definition-of-deployable.md` checklists. The "did it actually work" half lives in the checklist's Manual rows, requiring release-manager / on-call evidence.

**`UNVERIFIED` is not a pass.** A check that cannot run — e.g. `branch-protection.sh` with no `gh`/remote — exits **2** and is reported `UNVERIFIED`, distinct from PASS; in CI or under `--require` it escalates to a **FAIL**. A green dashboard hiding an unseen UNVERIFIED is the false assurance this layer exists to prevent.

**`INCOMPLETE` is not a pass either.** A run that was *interrupted* proves nothing about the checks that never ran — and a partial transcript looks a lot like one still in progress. **The completion signal is the terminal `RESULT:` line: if it is absent, the run did not finish.** Never read "no failures so far" as "passed".

> **Budget for it: the full aggregate is ~103 checks and takes ~5 minutes** (measured: 281s, v3.171.0). That is **longer than the default foreground command cap of most agent harnesses** — so when driving this kit with an agent, run `verify.sh` **without a command timeout**: background it, or capture output to a file and read the file. A cap that fires mid-run kills the aggregate at whatever check it had reached, which is exactly how CP-7 run 4 produced a stall that looked like a defect in `verify.sh` itself (it was not; see `docs/architecture/2026-07-21-cp7r5-k16-incomplete-design.md`).

A signalled run exits non-zero (`130` INT / `143` TERM) **and** prints `RESULT: FAIL (INCOMPLETE …)`, so neither a status check nor a human reading the transcript is misled. **Honest ceiling:** the announcement cannot fire on `SIGKILL`, and nothing can help a consumer that simply stops reading — which is why the runtime is documented above rather than left to be discovered.

In short: **green proves controls hold and safety is documented; it does not prove the documented procedures were tested.**

## Index

| Check | Type | Contract it proves | Gate |
|-------|------|--------------------|------|
| `version-helpers.sh` | script | shared semver helper lib (sourced by `meta-control-fresh.sh` and `version-tag-coherent.sh`) — not a gate itself | — |
| `version-tag-coherent.sh` | script | VERSION agrees with the git tag state — no skipped release bump (HEAD tagged → VERSION matches; VERSION ≥ highest reachable tag) | per-PR + drift-watch + tag-push |
| `runaway-killswitch-wired.sh` | script | runaway ceilings enforced — `scripts/runaway-guard.sh` exists, is executable, and breaches halt the loop (token / step / agent dimensions) | per-PR + drift-watch |
| `15-factor-checklist.md` | checklist | `DEVELOPMENT-STANDARDS.md` §13 (15-Factor Architecture) | Review (conditional) |
| `definition-of-deployable.md` | checklist | `DEVELOPMENT-PROCESS.md` §10 / §4 (release readiness) | Release (conditional) |
| `deployable-ready.sh` | script | `DEVELOPMENT-PROCESS.md` §10 — documented release-safety (RUNBOOK deploy/rollback + smoke); pairs with the checklist | Release / CI (conditional on a deploy surface) |
| `dr-readiness.md` | checklist | `DEVELOPMENT-STANDARDS.md` §10 / NIST 800-34 (DR is provable) | Review / recurring / DoD (conditional) |
| `dr-ready.sh` | script | `DEVELOPMENT-STANDARDS.md` §10 — documented DR (BIA + RUNBOOK §6 + recorded drill); escalate-only; pairs with the checklist | Review / CI (conditional on a data surface) |
| `resilience-readiness.md` | checklist | `DEVELOPMENT-STANDARDS.md` §4 / §6 (resilience + load/soak) | Review / recurring (conditional) |
| `resilience-ready.sh` | script | `DEVELOPMENT-STANDARDS.md` §4 / §6 — recorded resilience drills (RUNBOOK §8); pairs with the checklist | Review / CI (conditional on a deploy surface) |
| `observability-readiness.md` | checklist | `DEVELOPMENT-STANDARDS.md` Factor 14 / §3 (SLOs + telemetry observable in prod) | Review / recurring (conditional) |
| `observability-ready.sh` | script | `DEVELOPMENT-STANDARDS.md` Factor 14 / §3 — recorded observability posture (RUNBOOK §8 SLOs + telemetry); pairs with the checklist | Review / CI (conditional on a deploy surface) |
| `eval-ready.sh` | script | gate parity — the AI-feature **eval discipline is declared** (EVAL-PLAN + regression threshold + harness/gate recorded); conditional (N/A for non-AI). Does NOT run evals — that is the §7 Eval gate. Pairs with `eval-readiness.md` | Review / CI (conditional on an AI feature) |
| `responsible-ai-readiness.md` | checklist | RAI arc — AI System Card present + classified + oversight (US-anchored; EU optional) | Review / §7 (conditional on an AI feature) |
| `responsible-ai-ready.sh` | script | RAI arc — the AI-governance declaration is present (AI System Card + classification + oversight); conditional (N/A for non-AI). Does NOT judge correctness/fairness/compliance. Pairs with `responsible-ai-readiness.md` | Review / CI (conditional on an AI feature) |
| `test-data-ready.sh` | script | Safe Non-Prod — the test-data approach is recorded (RUNBOOK §2: synthetic/masked/never-raw-prod); conditional on a data surface. Pairs with `test-data-readiness.md` / `../docs/operations/test-data-management.md` | Review / CI (conditional on a data surface) |
| `test-layers-ready.sh` | script | integration + e2e test layers are present when a project has a service surface (stack-neutral); conditional (N/A for projects with no Dockerfile / compose service). Pairs with `../docs/operations/test-layers.md` | per-PR + drift-watch |
| `preview-env-ready.sh` | script | Safe Non-Prod — the preview-environment approach is recorded (RUNBOOK §4: per-PR isolated, safe data, auto-teardown); conditional on a deploy surface. Pairs with `preview-environments-readiness.md` / `../docs/operations/preview-environments.md` | Review / CI (conditional on a deploy surface) |
| `agentops-ready.sh` | script | MP-3 agentic-ops — the agent-run **trace discipline is recorded** (RUNBOOK §8 `Agent-ops:`); conditional (N/A for non-agentic). Does NOT verify traces emit or behavior conforms. Pairs with `agentic-ops-readiness.md` / `../docs/operations/agentic-ops.md` | Review / CI (conditional on an agentic project) |
| `security-policy.sh` | script | SP-2 — a governed repo (CLAUDE.md present) ships a `SECURITY.md` with a real security contact (not the `[security-contact]` placeholder); N/A for scratch dirs. Proves the disclosure policy is **recorded** — NOT that the process works (SLAs met, triage happens). | Review / CI (conditional on a governed repo) |
| `privacy-ready.sh` | script | SP-3 — a project that declares it handles Confidential/Restricted data (real `Data classification:` value, not the template placeholder) MUST carry a filled `PRIVACY-REVIEW.md` (DPIA-lite with a real lawful basis); Public/Internal/placeholder/none → N/A. Proves the privacy review is **recorded** — NOT that the processing is lawful or that deletion works (those are Manual rows). Pairs with `../docs/enterprise/data-governance.md` | Review / CI (conditional on a sensitive-data surface) |
| `obligation-lib.sh` | script (engine) | HITL-1 — the shared **obligation-trigger engine** sourced by per-obligation checks: `obl_changeset` (diff-derivation, `merge-base HEAD origin/main`, emitting NUL-separated **unquoted** paths since HITL-4 — bare `--name-only` quote-wraps non-ASCII paths and the trailing `"` defeated every extension surface glob, a silent fail-open on all three obligations; the fix is monotone, so no previously-triggering change-set can stop triggering — and **`--no-renames` since Slice B**, because git detects renames by default and emits **only the destination path**, so a `git mv` in the same PR removed the sensitive *source* path from the change-set and a triggering change derived N/A on all three gates at once: measured, an edit to `svc2/pii/export.py` FAILed in place and the same edit moved to `svc2/core/export.py` (R098) read N/A. Also monotone — un-collapsing a rename can only *add* the source path; measured across four real base/head pairs, a 300-commit sweep and two synthetic rename repos, **paths lost = 0**) · `obl_detect` (surface-glob heuristic → triggered/none/uncertain, with an optional **exclusion list evaluated before the surface globs** so an obligation detecting by markup extension can excuse a config template borrowing it — `OBLIGATION-CONFIG-TEMPLATE-EXT`) · `obligation_gate` (record present **and** filled; six **gate-defining** arguments — `--name`/`--surface-globs`/`--record`/`--template-marker`/`--stub-pattern`/`--exclude-globs` — are fenced by a `--` sentinel and first-assignment-wins, and the gate is **refused outright** if its surface defines no usable glob, if its exclusion list contains a glob matching every path, or if **any two-argument flag arrives with no value** — that last one used to abort the check with no `FAIL:` line from the production entrypoint and to spin the parse loop **forever** from any caller wrapping the gate in a condition, because `shift 2` fails and errexit is suppressed for that subtree). **The substance floor (`OBL_MIN_SUBSTANCE_LINES`, Slice B) is the headline engine change and lives here, not in the consumers:** a record must carry ≥1 markdown heading AND ≥8 non-blank lines, so `touch THREAT-MODEL.md` no longer satisfies **any** obligation — calibrated against the banner-stripped shipped templates (UAT-SIGNOFF 12, A11Y-SIGNOFF 17, THREAT-MODEL 45), not guessed, with a selftest leg that reds if a retune or a trimmed template puts the floor above what a template-derived record can reach. Its ceiling is equally the engine's: eight lines of prose passes, the floor measures **structure, never quality**, and the presence test `[ -f ]` **follows symlinks** — `ln -s README.md THREAT-MODEL.md` reads as filled (a *dangling* symlink correctly FAILs). Fail-safe: a non-derivable base or uncertain surface routes to *require the record* (fail-closed). Base-independent `--selftest` is mutation-tested (registered in `verify.sh`); the engine itself is not run diff-relative. Pairs with `threat-obligation.sh` | verify.sh selftest + non-vacuity sweep |
| `threat-obligation.sh` | script | HITL-1 / HITL-5 — the reference obligation on the engine: a **diff-relative** change touching a sensitive **or regulated-data** surface MUST carry a filled `THREAT-MODEL.md`; neither surface → N/A. Surface = the HITL-1 **sensitive** half (secret / auth / password / payment / `migrations/` / `.env`) **plus** the HITL-5 **regulated-data** half (`pii` · `gdpr` · `patient` · `cardholder` · `phi` · `ssn` · `personal-data`), held as two separate constants (`THREAT_LEGACY_GLOBS` + `THREAT_REGULATED_GLOBS`) so the HITL-5 addition is **structurally monotone** — the pre-HITL-5 surface still exists as its own value — and proved monotone twice over: the coverage leg probes all six legacy globs individually, and a load-bearing negative runs a regulated path against `THREAT_LEGACY_GLOBS` alone and requires N/A. HITL-5 closes the half this check's own title always claimed: `privacy-ready.sh` does **not** cover it, because that gate is **declaration**-triggered — a project that never recorded a `Data classification:` line stays N/A while a PR *introduces* PII handling. **Glob shape is measured, not chosen:** `obl_detect` matches with POSIX `case` (case-**sensitive**) and acronyms are conventionally uppercase, so the tokens are bracket-cased (`*[Pp][Ii][Ii]*`) — a lowercase-only glob would be a false *negative* on exactly the highest-consequence files (`PHI_export.ts`) — while `phi`/`ssn` are **segment-anchored**, because a bare `*phi*` measured **982** hits on a 162k-file corpus (`graphics.py`, `sphinxext.py`, `morphism.ts` — 0.6% of a typical tree) and a bare `*ssn*` **21** (`getElementsByClassName.js` — cla**ssn**ame); those four decoys are held N/A by a selftest leg forever. **The casing is applied to every letter of six of the seven tokens, and was not at first:** `patient`/`cardholder` shipped cased on their *first letter only* and `personal-data` uncased, so `data/PATIENT_EXPORT.csv`, `db/CARDHOLDER_DATA.sql` and `export/Personal-Data/dump.csv` all measured **N/A on the production path** while `src/PHI_EXPORT.ts` red-ed — the stated rationale applied to four tokens of seven. Repaired (owner-ratified) and re-measured before shipping: each of the three fully-cased forms scores **0** hits, and the regulated half's total is **11 before and after**, so the widening is free on real trees; the three ALL-CAPS paths are now the probes for those globs, while the lowercase forms stay covered by LEG 7. **Residual ceiling on the casing itself:** `personal-data` is cased per *word* (`*/[Pp]ersonal-[Dd]ata/*`), so an ALL-CAPS `PERSONAL-DATA/` directory is still N/A; the full-bracket form also measures 0 and is **boarded**, not taken, because widening past what was ratified is not a unilateral call. **Residual false positives, re-measured on a 243,157-path corpus of real trees and NOT what the design table recorded:** `*[Pp][Ii][Ii]*` → **8** (not 0 — that 0 was measured for the *bare* token, and the bracket casing added afterwards admits `…OpenA`**`piI`**`n…`, a common camelCase shape: `apiId`, `apiIndex`), `*-[Pp][Hh][Ii]*` → **3** (`about-philosophy.tsx` — the separator anchors the *left* side only). Both figures are from that one corpus, re-run for this commit; a reviewer's larger 266,727-path corpus scored `pii` one higher, which is why the corpus is named beside each number. **Diff-visibility, which the raw counts hide:** all **8** `pii` hits are vendored `node_modules` paths (svix's `eventTypeImportOpenApiIn.*`) that a gitignoring repo never puts in a diff, whereas **2 of the 3** `-phi` hits sit in `src/app/…` and would. The narrower `*pii*|*PII*|*Pii*` (0 hits) is **boarded**, not taken — it under-triggers on the highest-consequence class. Accepted under the owner-ratified **fail-safe posture** (for regulated data, over-triggering beats under-triggering) with the scale stated: 11 hits in 243,157 paths is 0.005%, against **482** for the already-shipped `*auth*` and **179** for `*secret*` on the same corpus — the regulated half is ~44× quieter than a glob this gate has shipped since HITL-1. **Honest ceiling of the regulated half:** it is a **path** heuristic, so a file holding PII under a neutral name (`src/models/user.rb`) is still N/A; `privacy-ready.sh` and review cover that. Proves the threat model is **recorded** — NOT that it is sound (review backstops). Composes with `privacy-ready.sh` (project-level, different axis — no double-charge). The base-independent `--selftest` runs **9 legs**: every one of the 19 surface globs carries a declared probe, asserted twice (through the wrapper, and against that glob **alone** — so a RED cannot be borrowed from a neighbouring glob), locked from both directions plus an exact count lock, so neither an added-unprobed nor a silently-deleted glob can ship. **Runs as a PR-context CI step (`fetch-depth: 0` so `origin/main` resolves), NOT in `verify.sh --require`** — the offline aggregate carries only its base-independent `--selftest`, because a diff-relative gate would false-red on a base-less shallow checkout or a fresh clone. Adopters **copy the reference step** — the `Threat-model obligation (PR-context …)` step in `../profiles/typescript-node/ci.yml` — into their own pipeline, where it **hard-enforces** because that step lives inside their required `conformance` job; the kit runs it on itself via the `threat-obligation` job in `../.github/workflows/ci.yml` (a standalone top-level job, so **advisory until added to branch-protection required checks — boarded** as `OBLIGATION-REQUIRED-CHECKS`). | PR-context CI (kit + copy-into-adopter-CI) · selftest in verify.sh |
| `uat-obligation.sh` | script | HITL-2 — the second obligation on the engine: a **diff-relative** change touching a user-facing **taste** surface MUST carry a filled `UAT-SIGNOFF.md`; no taste surface → N/A. Surface = the UI **directories** (`components/` · `views/` · `pages/` · `screens/` · `ui/` · `frontend/` · `styles/`), each listed **twice** — `x/*` and `*/x/*` — plus the UI extensions (`.tsx/.jsx/.vue/.svelte/.css/.scss/.less`) and the view-template **markup extensions**. Two repairs shipped in Slice B, both measured: **(1) `UAT-ROOT-DIR-GLOB`** — the shipped surface carried only the nested `*/x/*` form, and `*/x/*` requires a leading path segment, so a repo-root `components/Button.ts` (the **Next.js and Vite default layout**) was N/A and HITL-2/HITL-3 were effectively unenforced for a frontend-first adopter; masked only because `.tsx`/`.css` were caught by *extension* globs, so anything else walked through (`components/Button.ts`, `ui/icon.svg`, `views/index.php`, `frontend/main.ts`, `styles/theme.styl` all measured N/A before, RED after). **(2) `OBLIGATION-TEMPLATES-GLOB`** — the bare `*/templates/*` glob is retired for view-template markup extensions, the same repair `a11y-obligation.sh` already carries: a directory named `templates/` is not a UI signal (Helm/CloudFormation/cookiecutter), and **this repo's 14 `profiles/*/deploy/helm/templates/*.yaml` manifests were red-ing** — a QA acceptance sign-off demanded for a Kubernetes Deployment, a false positive with no honest way to satisfy it. Measured on a 243,157-path corpus: the swap **drops 759** paths from the surface (317 `.md`, 118 `.js`, 108 `.yaml`, 105 `.ts` — largely this kit's own `templates/` document set as adopters carry it) and **adds 4** — enumerated rather than left as a number: **3** are mypyc's `lib-rt/module_shim*.tmpl` **C-source shims** (a bare `.tmpl` that is neither a view nor a config template, and unreachable by a compound-suffix exclusion) and the 4th is `@vercel/analytics/dist/astro/index.astro`, a genuine Astro component and so a *correct* add. **Honest trade, not a strict win:** a view template whose extension is unlisted is now N/A (`.mako` — 12 measured — plus `.vm`/`.st`/`.dust`). **One deliberate divergence from `a11y-obligation.sh`, kept:** no `*.html`/`*.htm` — semantic HTML is a first-class *accessibility* surface, but a raw `.html` edit is not necessarily a change of *taste*. **Disclosed cost of keeping it** (a regression this change introduces, not an inherited limit): a Django/Flask/Rails view template at `app/templates/index.html` red-ed under the bare directory glob and is now N/A **for this gate** — verified still RED for `a11y-obligation.sh` on the true production path, so it escapes the taste ceremony, not the HITL ceremonies. **No `--exclude-globs`, deliberately:** this check inherits a11y's config-template-on-a-markup-extension false positive, but the **compound-suffix** slice of that class — precisely the 12 globs a11y's exclusion list enumerates (`*.conf.j2`, `*.yml.erb`, `*.sh.tpl`, `templates/_*.tpl`, …), which is all an exclusion list of that shape could remove — measured **zero** hits when run over that corpus directly. The wider class did *not*: the 3 mypyc `module_shim*.tmpl` shims above are a markup-extension false positive with no inner extension to key on, so no compound-suffix exclusion reaches them. Every such path this repo or that corpus actually holds *already* red-ed under `*/templates/*` (so no regression in that direction), and an exclusion list is an **off-switch** — shipping one here means either duplicating a11y's 12 entries **and** its ~60-line shape+coverage proof into a file that can then drift, or shipping the off-switch unproven. The right shape is one **shared** exclusion constant both checks read; boarded rather than bodged. Conservative by design (clear-UI globs only; ambiguous/non-UI → N/A), fail-closed on an uncertain/underivable change. The base-independent `--selftest` runs **9 legs**, **deriving** one probe per glob from the surface string (so an added glob cannot ship unproven and an unprobeable shape is itself a failure) under an **exact count lock**, asserting all 14 live Helm manifests N/A **on the kit tree** (an incepted adopter tree is pruned to its selected stack, so it carries 2 — or 0, since only 7 of the 10 profiles ship a chart; the exact count lock is a kit-tree property and a tree-independent fixture set carries the leg's teeth at any count) against a live view-template extension set, and rebuilding its filled-record fixture **from `templates/UAT-SIGNOFF-TEMPLATE.md`** rather than hand-writing it — the old fixture sat at 12 non-blank lines against the engine's floor of 8, the smallest margin in the suite, and by coincidence rather than coupling. Proves the acceptance sign-off is **recorded** — NOT that a human actually exercised the surface or that the acceptance judgment is sound (UAT sign-off + review backstop that). The HITL-3 `backlog-current.sh` Done-edge (a `Done` row flagged as a taste surface must name a `UAT-SIGNOFF`) is the **board-discipline** layer; **this** gate is the hard **diff-level** backstop. **Runs as a PR-context CI step (`fetch-depth: 0` so `origin/main` resolves), NOT in `verify.sh --require`** — the offline aggregate carries only its base-independent `--selftest` (a diff-relative gate would false-red on a base-less shallow checkout). Adopters **copy the reference step** — the `UAT-signoff obligation (PR-context …)` step in `../profiles/typescript-node/ci.yml` — into their own pipeline, where it **hard-enforces** because that step lives inside their required `conformance` job; the kit runs it on itself via the `uat-obligation` job in `../.github/workflows/ci.yml` (a standalone top-level job, so **advisory until added to branch-protection required checks — boarded** as `OBLIGATION-REQUIRED-CHECKS`). Pairs with `obligation-lib.sh`. | PR-context CI (kit + copy-into-adopter-CI) · selftest in verify.sh |
| `a11y-obligation.sh` | script | HITL-4 — the third obligation on the engine: a **diff-relative** change touching a user-facing **UI** surface MUST carry a filled `A11Y-SIGNOFF.md` **at the repository root**; no UI surface → N/A. Surface = the `uat-obligation` UI **directory** globs (each listed twice, `x/*` and `*/x/*`, so a repo-root `components/` is not missed), **plus** UI and view-template markup extensions, **minus** the bare `*/templates/*` glob (a `templates/` directory is not a UI signal — Helm/CloudFormation/cookiecutter all use it, and this repo carries 14 such Helm manifests). **Honest trade, not a strict win:** extension detection also catches view templates outside a `templates/` dir, but a view template whose extension is unlisted is now N/A, and config templates sharing a markup extension (Ansible `nginx.conf.j2`, Chef `app.conf.erb`, Helm's own `templates/_helpers.tpl`) used to be a known false positive — closed by `OBLIGATION-CONFIG-TEMPLATE-EXT`, which gave the engine an **exclusion list evaluated before the surface globs**; the entries are compound suffixes and `_`-prefixed Helm partials, never a bare extension, and the selftest holds that shape two ways — LEG 11 **asserts the shape of every entry** (anything else is refused; the engine's own refusal catches only a glob matching *every* path, which is far weaker — measured, `*/pages/*.tsx` substituted for a real entry passed all 13 legs green while deleting Next.js `pages/` from the surface), and LEG 12 holds real view templates on those same extensions still gated — **plus two canonical UI compound-suffix conventions the shape rule cannot protect.** The shape rule bounds an exclusion's blast radius (it can only match a final path component carrying at least two dots, so it can never delete a whole extension) but it does **not** decide config-vs-UI, and it cannot be made to: `*.stories.tsx` and `*.test.tsx` are the same shape and are arguably legitimate exclusions. Measured with the count lock held at 12, `*.component.html` (Angular), `*.module.css` (CSS Modules/Next.js), `*.module.scss`, `*.page.tsx`, `*.view.hbs` and `*.html.haml` each survived all 13 legs while deleting that convention from the surface — `src/app/hero/hero.component.html` read `N/A: no a11y surface touched` on the true production path. LEG 12 now holds the first two behaviourally; the other four still survive, and **review is the backstop** for that judgment. **Residual — a class, not a case:** any compound suffix not enumerated still triggers (`*.json.tpl`, `*.env.tpl`, `*.ini.j2`, `*.sh.j2`, `*.yml.erb`, `*.sh.erb` — the last two the commonest Chef shapes — all measured FAIL). The list is the mainstream cases, **not exhaustive**, and is deliberately not extended to chase the class: the class is unbounded, so no finite list makes it closed, and for an exclusion an *addition* is the dangerous direction. The bare `<name>.tpl` with no inner extension is the one case that could not be excluded even in principle — indistinguishable by path from a Smarty view template. Conservative by design, fail-closed on an uncertain/underivable change. Proves the WCAG 2.1 AA sign-off is **recorded** — NOT that the audit was competent, that axe/Lighthouse ran, that the verdicts are truthful, or that the record is **fresh for this change** (one committed record satisfies every later UI PR; review reads the record's Date). "Filled" is coarse but not trivially faked: the engine rejects the template banner, its stub tokens, and any record below `OBL_MIN_SUBSTANCE_LINES` non-blank lines or carrying no markdown heading (the substance floor, shipped in Slice B — it closes the `touch A11Y-SIGNOFF.md` bypass). Eight lines of prose under a heading still passes. **Deliberately a separate gate from `uat-obligation.sh` despite the near-identical surface:** a11y and taste are two *different judgments about the same surface*, enforcing different records signed by different roles (designer / a11y owner vs. QA / PO), so one must not mask the other. Turns the §14 conditional a11y gate from prose into enforcement. **Runs as a PR-context CI step (`fetch-depth: 0`), NOT in `verify.sh --require`** — the offline aggregate carries only its base-independent `--selftest`, whose 13 legs **derive** their probes from the surface-glob string *and* from the exclusion-glob string (so an added glob of either kind cannot ship unproven, an unprobeable glob shape is itself a failure, and every exclusion carries a load-bearing negative proving its probe is on the surface at all), **corpus-lock the surface against a declared list of real-world UI paths** (a derived probe always matches its own glob, so a *typo* self-validates — `*.tsxx` and `*/screen/*` both pass a shape rule; the corpus catches a glob matching nothing real, in both directions, count-locked. Honest ceiling: a typo that merely *widens* a glob while still matching a corpus path is NOT caught, and shipping a new dead glob is raised from one slip to two conscious acts, not made impossible — review is the backstop), and probe **every bracketed stub cell** of the shipped template, count-locked against its fillable-row count (a renamed token is caught by the generic pattern, a removed one by the count). Adopters **copy the reference step** — the `A11y sign-off obligation (PR-context …)` step in `../profiles/typescript-node/ci.yml`; the kit runs it on itself via the `a11y-obligation` job in `../.github/workflows/ci.yml` (a standalone top-level job, so advisory until added to branch-protection required checks — boarded as `OBLIGATION-REQUIRED-CHECKS`). Pairs with `obligation-lib.sh`. | PR-context CI (kit + copy-into-adopter-CI) · selftest in verify.sh |
| `ceremony-binding.sh` | script | LOOP-CEREMONY-BINDING Slice 1 — the **DESIGN GATE** backstop: a **diff-relative** Sensitive/Control-plane change MUST carry a design artifact that is (a) named by a GO record in `refs/notes/promotions` carrying **both** `gate: design` **and** `scope: <this PR>`, (b) a **design artifact** matching `CB_DESIGN_GLOB` (default `docs/architecture/*-design.md`) that is tracked, not a symlink and above the substance floor, and (c) **touched by the commit that GO approves** — `approved-sha:` must be a HEX OBJECT NAME (a revision expression like `HEAD` re-resolves at check time and binds nothing) and is normalised before comparison, since 27 of this repo's 61 real records are abbreviated. Ordinary → N/A, so XS and doc work never see it. Class comes from `promotion-readiness.sh --class` (single authority). **⚠️ IT MAKES NO ORDERING CLAIM.** The ordering predicate was **withdrawn** after being defeated five times in three adversarial rounds — reflexive ancestor (`git commit --amend`); a sixth path-derivation site (`git mv`); a touch-not-authorship anchor (zero-byte create, blank line, or mode change); the stale anchor merely relocated; and abbreviated-SHA false REDs. Each fix relocated the defect rather than closing the class, while every other component held across all three rounds. A design may therefore have been written **after** the work and this gate will not notice (boarded: `CEREMONY-ORDERING-PROOF`). **Honest ceiling:** proves a human recorded a scoped GO naming a substantive artifact — not authorship (a zero-byte create satisfies a touch), not quality, not that anyone read it (**PR #430 would have passed**); a note **binds but does not authenticate**, and `refs/notes/*` sits **outside branch protection**, so the storage of this control is weaker than what it gates. **Runs as a PR-context CI step (`fetch-depth: 0` + an explicit `refs/notes/*` fetch, which `actions/checkout` does NOT do), NOT in the offline aggregate** — the aggregate carries only its base-independent `--selftest` (23 legs, every negative asserting its **specific verdict text**; five vacuous legs were caught and fixed during the build, the last of them only by mutation). Verified under **dash as well as bash** — an unescaped `[` in a parameter expansion made the whole gate RED under `/bin/sh` on Linux and nothing else in the fleet caught it. A standalone top-level job, so **advisory until added to branch-protection required checks — boarded as `CEREMONY-BINDING-REQUIRED-CONTEXT`**. Pairs with `promotion-verify.sh record --gate design`. | PR-context CI · selftest in verify.sh |
| `egress-policy.sh` | script | Slice 11b — default-deny network egress is declared + attested-wired (three-state; UNVERIFIED-honest; never inspects traffic). Pairs with `egress-readiness.md` / `../docs/operations/egress-control.md` | Review / CI (conditional on a network surface) |
| `containment-ready.sh` | script | Slice 11c — agent-containment posture declared + attested-wired (sandbox FS · scoped tokens · separate prod creds; three-state, overall=weakest; never verifies enforcement). Pairs with `containment-readiness.md` / `../docs/operations/containment.md` | Review / CI (conditional on an integration/deploy surface) |
| `cost-governance-ready.sh` | script | H3b — cost-governance posture declared + attested-wired (per-run budget + platform spend-cap; three-state; never verifies spend was actually capped). Pairs with `../docs/operations/cost-governance.md` | Review / CI (conditional on a deploy/integration surface) |
| `assurance-tiers.sh` | script | Slice 11d — the compliance crosswalk states each Containment-arc control at its real responsibility tier (MCP gate = Kit-enforced; egress/sandbox/tokens/prod-creds = Kit-assisted); drift-guard against a silent revert. Verifies the tiers are *stated*, not "true" | CI / Review |
| `ci-gates.sh` | script | `DEVELOPMENT-STANDARDS.md` §14 (CI/CD Pipeline) — recognizes GitHub `id: gate-X` steps **and** GitLab `gate-X:` job keys; the contract is the gate-ids, the platform is open (`../docs/operations/ci-platforms.md`) | CI / Definition of Done |
| `check-links.sh` | script | Docs link integrity (`DEVELOPMENT-STANDARDS.md` §11) | CI |
| `agent-autonomy.sh` | script | `DEVELOPMENT-PROCESS.md` §13 (autonomy tiers) — guard denies a tier breach | PreToolUse hook / CI |
| `guard-core-sourced.sh` | script | `DEVELOPMENT-PROCESS.md` §13 — all guard consumers source one deny-matrix core (anti-fork); pairs with `agent-autonomy.sh` (`../docs/operations/runtime-guards.md`) | CI |
| `mcp-policy.sh` | script | Slice 11a — the MCP capability gate classifies correctly: un-allowlisted destructive/egress MCP tools denied (fail-closed), read-only allowed, allowlist/override honored | CI |
| `../scripts/preflight.sh` | script | beginner on-ramp (Slice 9f) — prerequisite check (jq/git/toolchain); `--selftest` regression-locks the detector | CI / pre-Inception |
| `inception-done.sh` | script | `DEVELOPMENT-PROCESS.md` §3 / `START-HERE.md` (the Inception gate) | CI (bootstrap-into-temp) |
| `profile-completeness.sh` | script | `profiles/_TEMPLATE.md` (every profile fills all 11 sections; companion ci.yml conformant) | CI |
| `profile-parity.sh` | script | STACK-PARITY — every **FILLED** service profile ships the full DoD capability set (feature-flags+provider seam · smoke · DR-if-data-backed · app observability · test pyramid · health · deploy/rollback+container). Three NAMED sets: **FILLED** (held to the bar) · **PENDING** (a known, board-linked gap; untracked gap → FAIL) · **EXEMPT** (non-service profile, with reason); a family-completeness lock FAILs any service profile born outside the fence. **Kit-self**: audits the kit's own `profiles/` (which ships to adopters but is pruned per-stack, so its presence can't distinguish a kit tree). It detects a non-kit tree by two **export-ignored** markers — an adopter tree (no `docs/ROADMAP-KIT.md` AND no `.github/workflows/golden-path.yml`) → **N/A** (exit 0), fail-closed if either is present. Honest ceiling: proves **presence+shape**, not runtime (golden-path runs the drills); v1-green = the PENDING set is empty. | verify.sh + CI + drift-watch |
| `stack-selection.sh` | script | Slice 9g / R7 — the stack-decision aid is complete (guide + per-profile Best-for/Avoid-when + a matrix row per profile); drift-guard | CI |
| `persona-artifacts.sh` | script | Slice 9i / R9 — the QA/Designer persona artifacts exist (TEST-PLAN, UAT-SIGNOFF, A11Y-SIGNOFF) and are named in the §2 persona table; drift-guard | CI |
| `dor-defined.sh` | script | Slice 9i-b — the Definition of Ready is enumerated in `CLAUDE.md`, referenced by the gate doc, and carried by the `FEATURE-REQUEST` intake; drift-guard | CI |
| `badge-version.sh` | script | Slice 9k — the README version badge equals `VERSION` (drift-guard with `--fix` sync) | CI |
| `agents-brief.sh` | script | Slice 9k — `AGENTS.md` exists, points at the canonical docs, and stays within the brief line-bound | CI |
| `conditional-gates.sh` | script | SP-1 / Slice 9j — §7 names the conditional gates (a11y / load / eval / SAST / license), trigger-bound not universal; drift-guard | CI |
| `../scripts/license-check.sh` | script | SP-1 — stack-neutral SBOM license-policy gate; flags denylisted copyleft + counts undetermined; `--selftest` regression-locked | CI (`--selftest`) / gate-license |
| `action-pinning.sh` | script | Slice 9j / H4b — SHA-pins every `uses:` across the kit's own `.github/workflows/` AND the canonical reference pipeline (the kit pins actions everywhere it runs them) | CI |
| `supply-chain-verify.sh` | script | H4b — the GitLab profile's by-download tools (syft/cosign/gitleaks) are checksum-verified before exec (no curl-pipe-to-shell); regression-lock. Pairs with `../docs/operations/tool-supply-chain.md` | CI |
| `gitlab-adoption-complete.sh` | script | H4a — the GitLab adopter guide covers all three governance areas (branch-protection + ratification + DORA) and the profile references it; drift-guard against silent rot. Pairs with `../docs/operations/gitlab-adoption.md` | CI |
| `branch-protection.sh` | script | `DEVELOPMENT-STANDARDS.md` §14 / `DEVELOPMENT-PROCESS.md` §12 — `main` is actually protected | CI (where gh can reach the API) |
| `tracker-contract.sh` | script | Slice 9h — a Jira instance satisfies the §6 work-item contract (six states + Size/Risk fields, live); three-state (UNVERIFIED without creds); the Only-Assignee claim is attested | CI (`--selftest`) / adopter (live) |
| `doc-budget.sh` | script | Slice 9k-b — the core governing docs stay within their post-trim line budget (no silent re-bloat); budgets raised only by a ratified PR | CI |
| `shellcheck.sh` | script | lints the kit's **maintainer-editable** shell (`scripts/`, `conformance/`, `hooks/pre-push`) at the error/warning floor (`-s sh -S warning`); regression-lock (that shell is shellcheck-clean). The control-plane guard under `.claude/hooks/` is excluded — agent-non-editable, and regression-locked behaviorally by its own deny-corpus conformance instead. Conditional on shellcheck installed — CI installs it so drift is always caught; `--selftest` verifies clean fixture passes + dirty fixture fails | CI |
| `ci-selftest-coverage.sh` | script | meta-check — every selftest-capable kit check (`conformance/*.sh`, `scripts/*.sh`, `hooks/pre-push`) is wired into `ci.yml`, so a check can't ship "existing but unenforced"; fail-closed, lists any unwired check. **Self-excluded** (a meta-check can't non-circularly verify its own wiring; its presence in `ci.yml` is a one-time maintainer bootstrap). `--selftest` proves it detects an unwired check, passes when all-wired, and ignores non-selftest scripts | CI |
| `onboarding-complete.sh` | script | the onboarding on-ramp is structurally present + wired — `ONBOARDING.md` names the 3 fluency lanes, the project-CLAUDE template carries `Operator fluency`, `operator-fluency.md` exists and AGENTS.md points at it, the TDD walkthrough exists. Completeness only — green means present + wired, NOT that the teaching "works" (the guard + gates are the enforced safety net); `--selftest` covers gap + complete fixtures | CI |
| `discovery-complete.sh` | script | the optional discovery layer is structurally present + wired — `discovery-loop.md` names all six loop stages, the FRAME/SHAPE guides + the two upstream templates exist, and `ONBOARDING.md` links the discovery door. Completeness only — green means present + wired, NOT that any discovery was good; `--selftest` covers gap + complete fixtures | CI |
| `governing-docs-current.sh` | script | DRIFT-1 — the **31 named** governing artifacts an agent reads to learn *how work is done here* (**every** `skills/*/SKILL.md` and **every** `agents/*.agent.md`, plus `START-HERE.md`, `ONBOARDING.md`, `CLAUDE.md`, `AGENTS.md`, `MAINTAINING.md`, `MATURITY.md`, `DEVELOPMENT-{PROCESS,STANDARDS}.md`, `promotion-contract.md`, `{harness-adapters,meta-control,release-tag}.md`, the PROJECT-CLAUDE + KIT-FEEDBACK templates) do **not** prescribe the **retired** AMBER `apply.py` control-plane hand-off (CP-8c abolished it; the **dev-clone** replaced it). The set is **named, never globbed** (it cannot silently shrink to zero); **a missing named member is a FAIL** (a rename cannot retire a file from the scan in silence); and the skills/agents families carry a **completeness lock** — a new skill or agent not in the named set FAILs, so nothing is born outside the fence. Policy is **zero-tolerance** (no in-set allowlist — the docs that legitimately *explain* the retirement, `retiring-conventions.md` + `runtime-guards.md`, sit deliberately outside the set). **Kit-only by design:** an incepted tree returns **N/A** — `incept` *renames* `CLAUDE.md`, and a presence check cannot see a **substitution** (the slot stays full and the green looks earned), and `apply.py` is a generic filename that would false-RED an adopter's own prose in an undroppable gate. **Honest ceiling:** it greps a *known* signature over a *named* set — a **recurrence latch for this retirement**, not a general "no retired convention anywhere" prover; a paraphrase avoiding both tokens escapes it, and green means the docs no longer *teach* the retired route, **not** that the dev-clone route is *followed*. `--selftest`: planted → RED · case-variant → RED · missing member → FAIL · set drift → FAIL · incepted → N/A · **empty scan set → FAIL** | CI |
| `mirror-current.sh` | script | P1.2-pre-b — the released `VERSION` on `main` is **published on the public mirror**. Publication is a purely **manual** act, so a tagged-but-unpublished release was not red — it was **invisible** (the mirror sat at `v3.122.0` while the kit reached 3.134.0: **12 releases**, 8 of them guard/control-plane, unnoticed). **Cron, not a PR gate** — publication happens *after* merge+tag, so a PR gate would be red on every merge and would cry wolf on the happy path (`release-tagged.sh`'s doctrine). Mirror URL is **single-sourced** from `publish-public.sh` and **fails closed** if absent — a currency check aimed at the wrong remote is worse than none. **Honest ceiling:** proves a **tag exists**, *not* that the mirror's **tree** matches the export (that is verified by `publish-public.sh` at publish time). Green = **current**, not **good**. Up to 7 days to notice. `--selftest`: current → GREEN · behind → RED · zero tags → RED · **unreachable → exit 2 (UNVERIFIED, never a pass)** · single-source stripped → FAIL closed | drift-watch (live) / CI (`--selftest`) |
| `mirror-tag-protection.sh` | script | P1.2-pre-b — released tags on the mirror are protected **at the forge** (an active tag ruleset on `refs/tags/v*` blocking force-push + deletion). `publish-public.sh`'s refusal binds the **tool**; only a forge rule binds a **human with push rights**. **Honest ceiling:** a repo **admin can lift the ruleset** — immutability is *enforced by the forge and merely attested here*; we claim tamper-**evident**, never tamper-proof. Exit **2 = UNVERIFIED** without admin creds (the branch-protection precedent — an absent credential must never read as a green). `--selftest` proves the JSON predicate: active tag ruleset → OK · disabled → FAIL · branch-only → FAIL · wrong rule → FAIL · no rulesets → FAIL | drift-watch (live) / CI (`--selftest`) |
| `audit-evidence-checklist.md` | checklist | enterprise addendum (`../docs/enterprise/`) — per-control audit evidence | Review / pre-audit |
| `container-supply-chain.sh` | script | `DEVELOPMENT-STANDARDS.md` §14 (conditional container image supply-chain) | Review (conditional on a Dockerfile) |
| `backlog-adapters.sh` | script | `DEVELOPMENT-PROCESS.md` §6 (named backends agree across incept / §6 / the adapter guide) | CI / Review |
| `guard-wired.sh` | script | `DEVELOPMENT-PROCESS.md` §13 — the `.claude/` runtime guard is actually wired (fail-closed; gates Inception) | CI / Inception |
| `waivers-valid.sh` | script | `DEVELOPMENT-PROCESS.md` §13 / `DEVELOPMENT-STANDARDS.md` §14 — brownfield `WAIVER-REGISTER.md` is well-formed (no expired / non-negotiable / over-90d / missing-field waivers); N/A without a register | Review / CI (adoption-conditional) |
| `operate-loop-wired.sh` | script | operate-loop Slice 1 — `scripts/postmortem.sh` exists; `postmortem --selftest` exits 0; dispatcher routes `sparkwright postmortem --selftest` | CI |
| `doctor-wired.sh` | script | P3T4 — `scripts/doctor.sh` + `scripts/sparkwright` exist; `doctor --selftest` exits 0; dispatcher routes `sparkwright doctor --selftest`; rejects unknown commands with exit 2 | CI |
| `verify.sh` | script | the honest **aggregate** — runs the checks, labels each **control** vs **documentation**, gates on control failures; prints what a green run does and does not prove | CI (`--selftest`) / Review |

> The enterprise addendum (`../docs/enterprise/`) adds the compliance crosswalk and this audit-evidence checklist.

> **Note on `inception-done.sh` at the kit root:** this gate is *expected to FAIL* when run against the kit's own repository — the kit is the reference/template **source**, not an instantiated project (it has no `ADR-000`, `RUNBOOK.md`, etc.). It passes only inside a project that has completed Inception. Do not "fix" the kit root to satisfy it.

> **Progressive delivery (reference, no separate check):** `definition-of-deployable.md`'s progressive-delivery + smoke-gate rows pair with [`../docs/operations/progressive-delivery.md`](../docs/operations/progressive-delivery.md) for the *how* (canary/blue-green + smoke gates at every promotion boundary). The checklist is the conformance; the reference completes the triad.

> **DORA metrics (measurement-enablement, no gate):** §14's DORA four + agentic signals are *collected*, not gated — `../scripts/dora.sh` (GitHub-derivable subset; CI-smoked via `--selftest`) + [`../docs/operations/dora-metrics.md`](../docs/operations/dora-metrics.md) (derivation + the maturity-gating path). Value-gating is a §9 maturity step, not a baseline check.

> **Conditional checks — N/A semantics differ by blast radius.** `deployable-ready.sh` and `resilience-ready.sh` **skip-pass** cleanly when a project has no deploy surface — a miss is acceptable (it isn't a deployable service). `dr-ready.sh` is **escalate-only**: its `N/A` is *self-incriminating* (data detection is conservative, and a missed data project is a false negative with data-loss stakes), so `dr-readiness.md` applies regardless of what the script prints. The N/A weight matches the blast radius.
