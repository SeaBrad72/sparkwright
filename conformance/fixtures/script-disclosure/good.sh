#!/bin/sh
# good.sh — fixture: a well-formed kit script header for the disclosure gate.
# What it changes: read-only — this is a fixture; it mutates nothing.
# Guardrails: fixture only; never run in production; no network, no writes.
echo "fixture: good"
