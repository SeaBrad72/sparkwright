#!/bin/sh
# missing-changes.sh — fixture: header carries a purpose + Guardrails but OMITS the
# 'What it changes:' label (must FAIL — load-bearing negative for the changes label).
# Guardrails: fixture only; no network, no writes.
echo "fixture: missing changes"
