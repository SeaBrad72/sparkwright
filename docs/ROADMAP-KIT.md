# Kit Roadmap — Remaining Slices

The kit's **own backlog** (dogfooding `DEVELOPMENT-PROCESS.md` §6). The Foundation increment (the original `v1.0.0` release) established the meta-layer; the kit is now on the **3.x line** (release-line note below). Each remaining slice ships as a **contract → reference → conformance** vertical (`MAINTAINING.md` §1), in priority order, each with its own spec → plan → build.

## ★ Reprioritized backlog (2026-06-23 — authoritative order)

A 4-dimension adversarial audit (`docs/architecture/2026-06-23-e-series-consolidation-audit.md`) found the kit was accreting rigor faster than it validated fit — locally-good slices drifting globally. **Pivot: consolidate before E3; elevate a cadenced meta-control above feature epics so this can't recur.** This list supersedes the build-order tables below.

> **★ Order ratified by the meta-control first run (2026-06-23, GO-WITH-CONDITIONS).** M's first panel run resequenced this list *by the mechanism, not by assertion* — verdict + two ledgers at `docs/architecture/2026-06-23-meta-control-first-run.md` (logged in `docs/governance/meta-control-log.md`). Key changes it produced: **T2 pulled first of the T-series**; two **CI-trust Blockers** elevated into T4 (per-PR CI ran `verify.sh --selftest` not `--require`; claims-registry swallowed diagnostics); **E4d decoupled** from E3; **E3 no longer default-first** (per-epic M ratification governs feature order).

1. **T1 honesty — ✅ COMPLETE.** F1 ✅ (v3.47.1, file-count fix+lock); F3 ✅ + F4 ✅ (v3.47.2 — README `## Maturity & validation status` + EXEC-BRIEF §5; downscoped 4 CHANGELOG "PROVEN" headlines + claim-verb convention). *Residue the panel flagged → cheap T1 follow-on: unsourced EXEC-BRIEF/ROI stats; "first-class" 10 profiles unqualified **✅ closed by T3c profile relabel, v3.48.1**; `adapters/README.md` omits codex/cursor/gemini.*
2. **M — cadenced meta-control.** **M1 ✅ (v3.48.0):** harness-neutral panel runbook (`docs/operations/meta-control.md`) + Kit-Steward agent + verdict log + first run. **M2 ✅ ENFORCEMENT COMPLETE** — S1 `3.48.14` (export-hygiene + link-safety lock fix) · S2 `3.48.15` (freshness gate `conformance/meta-control-fresh.sh`: DUE = >N=5 tags since last addressed run; marker+log sync; applicability = detected trigger not declared mode; satisfied by run OR DEFERRED; weekly drift-watch enforcement + advisory doctor; per-PR --selftest only) · S3 `3.48.16` (`.claude/agents/*` → guard control-plane) · **S4 first real panel run** (GO-WITH-CONDITIONS, `docs/architecture/2026-06-25-meta-control-3.md`). **★ The S4 panel found the breaker is *self-certifiable* (verdict log + marker are not control-plane) → `M2-S5` ratification-integrity is the NEXT slice, BEFORE E4d** (make marker+log control-plane; reject future-pinned markers; cap serial-DEFERRED; lock all three). The panel also surfaced **adopter export RED on `verify --require`** (pre-M2; golden-path-coupled checks hard-require an export-ignored workflow) → high-priority standalone fix.
3. **T-series (ratified order):** **T2 ✅ DONE (real validation, 2026-06-23)** — cold-adopter run of v3.48.0 + solo/team governance validation; findings at `docs/architecture/2026-06-23-t2-real-validation-findings.md`. **Validated-as-quality:** green-on-clone CONFIRMED (zero over-promises — F1/F3/F4 hold under execution); solo governance bundle works (agent-review genuinely adversarial; branch-protection blocks self-merge; FLOOR logic 9/9). Team path = **logic-verified, live-deferred → `T2-team-live`** (below). → **T3 — right-weight ✅ DONE (T3a–T3d, v3.48.1–v3.48.6)** — assessment (`docs/architecture/2026-06-24-t3a-rightweight-assessment.md`) + content-preserving cuts (profile maturity relabel · 6 dead E4 design docs retired · SPEC→FEATURE-REQUEST *Extended spec* · CODE-REVIEW-CHECKLIST→REVIEW-RECORD rubric · frame/shape→discovery-loop sections) + epic reorg & release-line (items 5–6 above) + `docs/operations/retiring-conventions.md` (T3b descoped to a convention). **Kept by design-intent reversal:** AI-POLICY (incept governance set), JIRA-SETUP (server-enforced tier), the profiles' floating action pins. *Residual T2/T3 finding → T4: mode-dial `prototype`≡`team` differentiation + route-scaffolding.* → **T4 — conformance/UX hardening**, incl. the **two elevated CI-trust Blockers** (run `verify.sh --require` per-PR + assert ≥1 control PASS in `--selftest`; un-swallow claims-registry diagnostics + preserve three-state) · widen golden-path path-filter (F7) · extract selftest harness → `wf-helpers.sh` · conformance-carve **+ T2 findings: CODEOWNERS brownfield false-alarm on clean greenfield install, `explain` blind to process-vocabulary, the private-repo `enforce_admins` honesty note**. **+ T3-banked: the `check-links.sh` code-span gotcha** (it greps raw text, so a doc that *quotes* Markdown link syntax — bracket-then-paren — false-fails; it should skip backtick/fenced code spans) **· `docs/architecture/` ships to adopters** — candidate to export-ignore the kit's internal design/audit history (`.gitattributes` + `adopter-export.sh` IGN + the `adopter-export-wired` lock; an S3-class control-plane slice).
   - **★ `T2-team-live` (near-term, ~next):** the live team-governance validation T2 deferred — real `enforce_admins` + true 2-human non-author approval + the one-flip-upgrade end-to-end + the solo-discoverability probe (does a blocked cold adopter find `review-lane.md`?). Needs a **second forge identity + an org/paid repo** (`enforce_admins` 404s on private free-tier). Bradley provisions; then run.
4. **Before any feature epic — in order: adopter-export-RED fix → E4d** (★ `M2-S5` ratification integrity ✅ built/applying, v3.48.17). **M2-S5** closed the self-certification gap the S4 panel confirmed: the marker + verdict log are now control-plane in **both** guard matchers (tool `is_control_plane_path` + shell `guard_check_command` + the bare-basename net — a tool-only fix would have left the `printf > marker` shell back door open); future-pin guard (marker ≤ `VERSION`, which allows the legit ship-seam where marker == the about-to-ship version pre-tag); serial-`DEFERRED` cap (≥2 → OVERDUE); locked in `--selftest` (14 fixtures) + `agent-autonomy` (tool+shell). Dual-approved (reviewer + security-reviewer).
   - **★ TRACKED follow-on `M2-ratification-hardening`** (security-reviewer condition, 2026-06-25) — **valid ONLY while `docs/governance/.meta-control-last` + `meta-control-log.md` remain control-plane** (if a future change relaxes that, these become live self-cert paths again): (a) future-pin shape "marker ≤ max(`VERSION`, highest real tag) **and** marker is a real tag OR == `VERSION`" (allow exactly the one unreleased ship-seam version — NOT a naive ≤max-tag, which regresses the seam); (b) verdict case/enum (`deferred` lowercase evades the serial cap → uppercase-normalize + validate vs `GO|NO-GO|DEFERRED`); (c) CODEOWNERS on `docs/governance/` to make the human-commit airtight; (d) `ver_gt` helper dedup. All **backstopped** by the M2-S5 closure (the agent cannot author the marker/log at all) → non-blocking; route after adopter-RED.
   - **Adopter-RED**: `verify --require` is RED on a fresh export — `feature-flags-wired`/`containment-audit`/`runtime-security` hard-require the export-ignored `golden-path.yml`; make them N/A-when-absent (mirror `meta-control-fresh`'s applicability) + add `verify --require` to `adopter-export-wired.sh`. **E4d** (cost/runaway kill-switch) decoupled from E3, standalone (safety gap that grows with every epic).
5. **Feature epics — ratified order (meta-control panel #4, 2026-06-26, GO-WITH-CONDITIONS; `docs/architecture/2026-06-26-meta-control-4.md`):** **E3 is the spine; lead it with the *thin inputs it consumes*, then fan breadth out through proven E3.** Order: **(0)** trivial residue — explicit `/docs/governance/` CODEOWNERS line · **(1) E1-thin** (one integration + one e2e layer, gated — the oracle E3 consumes) · **(2) E5-thin ✅** (one OTel-style trace + one real scorecard — the sensor E3's runs need; v3.51.0) · **(3) E3, as 5 M-gated slices**: E3a thin loop **+ wire `runaway-guard.sh step` (the kill-switch has zero live callers today)** → escalation/human-in-the-loop ✅ SHIPPED (ex-E14; v3.55.0; **candidate follow-on: tier-checkpoint preemptive approval — completes the autonomy-metering thread (`tier-advice` is the advisory half); gate at a future per-epic M brainstorm after E3b/E3d, on real demand**) → orchestration mechanics (§10 item 6 conflict-safe writes ✅ SHIPPED v3.56.0; item 1 FS-isolation = harness-sandbox posture, deferred) → phase→agent flow → agent-memory **last** (ex-E12) · **(4) T2-team-live** (after E3a+escalation; live enforce_admins + 2-human approval + **solo-discoverability probe**; provision the 2nd identity/org repo in parallel) · **(5) E5-full** · **(6) E6** AI-native depth · **(7) E1-full** breadth · **(8) E9-vertical then E11-vertical** (≤2 slices each; scope-confirmed at per-epic M brainstorm) · **(9) E10** capstone · **(10) R** refactor. **★ The panel's substantive override:** DON'T "build E1/E5/E6 *through* E3" (circular — validator built by the thing under test, the F5 build-ahead-of-need error one level up); a *thin* E1+E5 must LEAD E3. **★ Prior T3d reorg (2026-06-24, still in force):** E12+E14 **folded into E3** (slices, not epics); E13 **dissolved** into E4d (done) + E6; E8 **deferred**; E9/E11 **kept but shrunk to verticals** (design-intent check: not redundant, not dead — scope-shrink ≠ drop). Each epic enters Build only on an affirmative per-epic M verdict. **Parallel/standing (not in sequence):** resolve the `prototype≡team` mode-dial dead-letter (C1, confirmed false-promise); claims.tsv honesty qualifiers (B1–B3); verify.sh `[control]`-label honesty (D1).
6. **Capstone:** **E10** holistic "is this too much?" + one comprehensive end-to-end validation on the finished kit + maintenance model · **R (NEW)** deliberate area-by-area refactoring model+plan, executed last (subtractive, *not* a rewrite; small because of continuous right-weighting).

**★ Release-line (ratified T3d, 2026-06-24):** stay on the **3.x line**; a **1.0 is premature**. T2 established the kit is **pre-adoption** (n=2 synthetic, no external human). Gate 1.0 on **feature-complete + E10 + a real external adopter** — not on internal milestone count. The right-weighting being *modest* (T3a: bones sound, no enforcement-surface cuts) supports a confident 3.x cadence, not a defensive rewrite.

**Done:** E2 (v3.41.0) · E4a–E4e containment (v3.42–v3.47.0) · T1-F1 (v3.47.1) · **T1-F3+F4 (v3.47.2) → T1 COMPLETE** · **M1 cadenced meta-control panel + first run (v3.48.0)**. **Parked:** G8 (workarounds; E4f killed) · dotnet NuGet-lock Q · stray feedback-triage `v3.37.0` tag.

---

| Order | Slice | Contract (mostly written) | Reference implementation to build | Conformance check |
|-------|-------|---------------------------|-----------------------------------|-------------------|
| 1 ✅ | **CI/CD** *(shipped v2.0.0)* | standards §14 + process §10/§15 | `profiles/typescript-node/ci.yml`, `CODEOWNERS`, `BRANCH-PROTECTION.md`; kit-own `.github/workflows/ci.yml` | `conformance/ci-gates.sh` |
| 2 ✅ | **Agent governance** *(shipped v2.1.0)* | process §13 + enforcement-reference note | `.claude/` — `settings.json`, `hooks/guard.sh`, `reviewer` + `security-reviewer` subagents, `README.md` | `conformance/agent-autonomy.sh` |
| 3 ✅ | **Inception bootstrap** *(shipped v2.2.0; absorbed templates)* | START-HERE 8-step gate | `scripts/incept.sh` + `RUNBOOK-TEMPLATE.md` + flow-board `BACKLOG-TEMPLATE.md` | `conformance/inception-done.sh` |
| ~~4~~ | **Template fixes** *(absorbed into Slice 3, v2.2.0)* | DoD + process §6 | RUNBOOK-TEMPLATE.md + flow-board BACKLOG-TEMPLATE.md shipped | covered by `inception-done.sh` |
| 5 ✅ | **Enterprise profiles** *(v2.3.0 → v2.5.0)* | `profiles/_TEMPLATE.md` | Python, Java/Spring (v2.3.0); .NET, Go, Rust, Kotlin + BYO `new-profile.sh` (v2.4.0); **ML — eval-gate-centric (v2.5.0)** | `conformance/profile-completeness.sh` |
| 5c2 ✅ | **Data-engineering profile** *(shipped v2.6.0)* | `profiles/_TEMPLATE.md` | `profiles/data-engineering/` — dbt + Dagster + Python; `gate-data-quality` (dbt build + Great Expectations) | `conformance/profile-completeness.sh` |
| 5d ✅ | **Terraform/IaC profile** *(shipped v2.7.0)* | `profiles/_TEMPLATE.md` | `profiles/terraform/` — Terraform + tflint + Checkov + conftest/OPA + Trivy; `gate-policy`; §14 via IaC analogs | `conformance/profile-completeness.sh` |
| 5e ✅ | **CI hardening** *(shipped v2.8.0)* | standards §14 (hardening note) | all 10 `profiles/*/ci.yml` — least-privilege OIDC (push-only provenance job), checkov pin | `conformance/ci-gates.sh` + `profile-completeness.sh` |
| 6a ✅ | **Compliance crosswalk** *(shipped v2.9.0)* | standards §2/§14 | `docs/enterprise/{README,compliance-crosswalk}.md` — SOC 2 + ISO 27001:2022 + privacy family | `check-links.sh` + audit-evidence (6d) |
| 6b ✅ | **Secrets at scale** *(shipped v2.10.0)* | standards §2 | `docs/enterprise/secrets-at-scale.md` + §2 contract + `_TEMPLATE.md` pointer | `check-links.sh` |
| 6c ✅ | **Ratification RBAC** *(shipped v2.11.0)* | process §12/§13 | `docs/enterprise/ratification-rbac.md` + §13 roles/exception contract | `agent-autonomy.sh` + audit-evidence (6d) |
| 6d ✅ | **Audit-evidence capstone** *(shipped v2.12.0; `v3.0.0` milestone)* | umbrella §4d | `conformance/audit-evidence-checklist.md` — per-control evidence, ties to 6a | `check-links.sh` + the checklist itself |
| 7a ✅ | **Environments & prod safety** *(shipped v2.13.0)* | process env model + standards §14 | Dev/QA/UAT/Prod + env-aware `guard.sh` + `branch-protection.sh` | `agent-autonomy.sh` + `branch-protection.sh` |
| 7b ✅ | **Multi-persona touchpoints** *(shipped v2.14.0)* | process §2/§5/§15 | persona map + `FEATURE-REQUEST`/`SPEC` templates + persona-routed START-HERE | `check-links.sh` (+ `inception-done.sh` no regression) |
| 7c ✅ | **Containers & image supply-chain** *(shipped v2.15.0)* | standards §14/§13 + process §9 | ts-node Dockerfile/compose/devcontainer/k8s/Helm + conditional image gate | `container-supply-chain.sh` + `ci-gates.sh` (8 ids intact) |
| 7d ✅ | **Work-tracking adapters** *(shipped v2.16.0)* | process §6 | `docs/work-tracking/adapters.md` (6 trackers + BYO) + incept `--backlog` set + template | `backlog-adapters.sh` + `check-links.sh` |
| 7e ✅ | **Brownfield & `.claude/` hygiene** *(shipped v2.17.0)* | process §13 (guard) | `docs/adoption/brownfield.md` + `.claude/` scoping + incept warn | `guard-wired.sh` (gates Inception) + `check-links.sh` |
| 7f ✅ | **Doc refresh & consistency** *(shipped v2.18.0)* | — (docs only) | ratification-role casing + 10-profile count + doc-set tables + inception-done note | `check-links.sh` + casing grep |
| 8a ✅ | **Incident Response standard** *(shipped v2.19.0)* | standards §15 + process §8/§9 | §15 Incident Response + `POSTMORTEM-TEMPLATE.md` + dangling-ref fixes | `check-links.sh` + audit-evidence (Manual row) |
| 8b ✅ | **Definition of Deployable** *(shipped v2.20.0)* | process §7/§4/§10 (release readiness) | `definition-of-deployable.md` + `deployable-ready.sh` (conditional, --selftest) | `deployable-ready.sh --selftest` + `check-links.sh` |
| 8c ✅ | **DR / backup-restore + BIA** *(shipped v2.21.0)* | standards §10 + process §7/§15 + DoD (NIST 800-34) | drill reference + `BIA-TEMPLATE` + `dr-readiness.md` + `dr-ready.sh` (escalate-only) | `dr-ready.sh --selftest` + `check-links.sh` |
| 8d ✅ | **Resilience + load/soak verification** *(shipped v2.22.0)* | standards §4/§6 + process §7 (chaos/SRE) | `resilience-verification.md` + `resilience-readiness.md` + `resilience-ready.sh` (conditional, --selftest) | `resilience-ready.sh --selftest` + `check-links.sh` |
| 8e ✅ | **Progressive-delivery + smoke gates** *(shipped v2.23.0)* | process §10 + standards §14 + 8b checklist | `progressive-delivery.md` + §14 smoke-gate + tightened deployable smoke row | `check-links.sh` + the (tightened) Definition-of-Deployable checklist |
| 8f ✅ | **DORA metrics collection** *(shipped v2.24.0)* | process §14/§9 (DORA + maturity-gating) | `dora-metrics.md` + `scripts/dora.sh` (GitHub-derivable subset, graceful degradation, --selftest) | `dora.sh --selftest` (CI smoke) + `check-links.sh` |
| 6 ✅ | **Enterprise addendum** | standards §2 (partial) | compliance-control mapping (SOC2/ISO), secrets-at-scale (Vault/KMS) patterns, RBAC for ratification | `conformance/audit-evidence` checklist — enterprise addendum complete (6a–6d), v3.0.0 milestone |

## Notes
- **Slice 8 shipped in v2.24.0** (incident response · definition of deployable · DR/backup-restore · resilience+load · progressive delivery · DORA — the continuity & safe-delivery arc, complete).
- Order matches the "CI first" priority: governance is only *enforced* once CI and the agent layer are wired.
- Slices 1–2 convert the kit from *described* governance to *enforced* governance — highest leverage. **Slice 1 shipped in v2.0.0; Slice 2 in v2.1.0 — that conversion is now complete.**
- Re-prioritize at the kit's L2/L3 retros; this order is the default, not a commitment.

---

## Post-2.62.0 fix-forward backlog (from the pre-launch go/no-go arc)

The pre-launch go/no-go (8 adversarial rounds) reached **0 blockers on the supported path**. **Most of this backlog shipped in 2.62.1** (Docker-verified where marked); the rest remains fix-forward. None breaks the verified `typescript-node` path or a headline claim.

**Highs — ✅ all closed in 2.62.1:**
- ✅ **go** — shipped `.golangci.yml` (govet/staticcheck/errcheck/gosec) + pinned `golangci-lint` version + `newServer()` refactor for gosec G114. *(Docker-verified)*
- ✅ **java-spring** — OWASP dep-scan now caches NVD + accepts optional `NVD_API_KEY` + first-run caveat.
- ✅ **kotlin** — `gradle wrapper` step pinned to `--gradle-version 8.10`.

**Mediums:**
- ✅ **ts-node** Dockerfile `HEALTHCHECK` — added `src/healthcheck.ts` + fixed the distroless node path (`/nodejs/bin/node`). *(Docker-verified: container `healthy`)*
- ✅ **dotnet** — added `.editorconfig` + `Directory.Build.props`; Dockerfile publishes the app project only + drops the bad root lockfile COPY. *(Docker-verified: 0 warnings)*
- ✅ **incept** scaffold-copy now skips stray build artifacts (`node_modules`/`dist`/`coverage`/`__pycache__`/`.coverage`/`target`/`bin`/`obj`/…).
- ⬜ **dep-scan prod-scoping consistency** *(remaining)* — ts uses `--omit=dev`; python `pip-audit` and java/kotlin OWASP audit all scopes. Unify the prod-dep posture + add a non-blocking dev-advisory audit. *(Cross-stack mechanic change; ts already prod-scoped — lower value, deferred.)*

**Lows/Nits (remaining, fix-forward):** gate-sast `--config auto` network/Pro-rules caveat note · rust `llvm-tools-preview` component note · java-spring Dockerfile `mvnw`-wrapper comment · non-ts reference `ci.yml` SHA-pin (vs major-float) for parity with ts-node · run `mvn wrapper:wrapper && ./mvnw verify` once to convert java-spring from "authored" to "maintainer-verified."

---

## Harness-neutrality arc (→ `3.0.0`)

Make the kit **LLM/harness-neutral** — anyone can pick it up with any agent harness out of the gate, while **Claude Code stays the default** and is regression-locked by its existing conformance. Full adapter model; first target = the generic/AGENTS.md baseline; **split proof bar** (enforcement maintainer-verified, process authored-to-contract). Design spec: `docs/superpowers/specs/2026-06-17-harness-neutrality-design.md`. Ships as additive minors; `3.0.0` is cut at N4.

| Slice | Ships | Status |
|-------|-------|--------|
| **N1 — `agent-boundary` CI gate** | harness-independent control-plane-ratification gate + `conformance/agent-boundary.sh` + reference job + §13 clause | ✅ **shipped 2.63.0** |
| **N2 — adapter contract + `harness-adapter.sh`** | boundary-contract doc + adapter manifest + composing conformance check + name `.claude/` the `claude-code` reference adapter | ✅ **shipped 2.64.0** |
| **N3 — `generic` adapter + `incept --harness`** | generic/AGENTS.md adapter + `--harness` flag (default `claude-code`) + per-harness verification + Inception-Done enforcement | ✅ **shipped 2.65.0** |
| **N4 — proof + positioning → cut `3.0.0`** | enforcement-evidence doc (the 3 CI-locked surface selftests) + BYO adapter `_TEMPLATE` + `new-adapter.sh` + stack/harness-neutral positioning + **release `3.0.0`** | ✅ **shipped 3.0.0 — harness-neutrality arc COMPLETE** |

---

## Post-3.0.0 backlog (consolidated + prioritized)

Sources: the 11-dimension adversarial go/no-go on 3.0.0 (**GO-WITH-CONDITIONS**, 0 blockers), the post-3.0.0 weight/usefulness review, the solo-vs-team review discussion, and a proactive hardening/edge-case sweep. **Burn-down order** (default — re-prioritize at a retro): **P1 ✅ → H1 → P2 → H2 → D1 → H3 → H4 → P3 → P4.** Hardening the *enforcement model* (H1) outranks usability polish — a kit whose own gates the agent can quietly disable is worse than a slightly heavier, tamper-resistant one. None below breaks the verified `typescript-node` path or a headline claim.

### P1 — Integrity & honest enforcement ✅ *shipped 3.1.0*
N5 `controlPlanePaths` union enforcement (the gate now denies unratified changes to the union of adapter-declared paths) + profile parity (python `fail_under=80`; GitLab `gate-eval`) + conformance hardening (branch-protection advisory; honest guard-bypass-class enumeration).

### H1 — Enforcement integrity ✅ *shipped 3.2.0* — the kit's own controls now resist the agent they govern
The enforcement layer (`conformance/`, `adapters/`, the named `scripts/`, `scripts/fixtures/`, and the governing docs `DEVELOPMENT-STANDARDS`/`PROCESS` + `CLAUDE.md`) is now in `guard-core.sh::is_control_plane_path`, so weakening a gate, relaxing the Definition of Done, or adding an adapter requires ratification (**H1.1**) — `scripts/` is a named-script set so an adopter's own `scripts/` code is untouched. `harness-adapter.sh` executes `proof.check` only from a vetted `conformance/*.sh` allowlist, rejecting metacharacter/traversal strings *before* execution (**H1.2**). The agent-forgeable `ratified-control-plane` label is removed — ratification is a non-author approval; solo = logged admin-merge (**H1.3**). The kit now dogfoods the real `gate-agent-boundary` job on its own PRs (**H1.4**); an unratified control-plane diff shows the check **red** (`exit 1`) meaning *ratify me* — the "ratification required" semantics ride naming + docs since GitHub has no non-failing blocking state for a plain step (live-verified on PR #110). **D4-presentation (3.6.0, PR #114) replaced the red `exit 1` with an `action_required` merge-gate** — the job exits 0 and posts a `control-plane-ratification` check that blocks the merge in amber with no "CI failed" email (red/email reserved for real failures; live-verified). Honest ceilings: the command-string guard layer is unchanged (a `python -c`/script control-plane edit isn't caught inline — the CI gate is the post-hoc backstop); command-string false-positive tuning → P2/WS1; GitLab gate parity → H4.

### P2 — Usability & governance ergonomics (light, not weak) ✅ *complete (WS1–WS4 shipped 3.3.0–3.7.0)* — *design spec: `docs/superpowers/specs/2026-06-17-p2-usability-governance-design.md`*
*(NOTE: "opt-in/modular enterprise layers" from the first backlog draft is **REJECTED** — conditional-applicability already auto-skips inapplicable controls; opt-in would re-create the add-compliance-later trap. The fix is friction-removal + surfacing, never weakening an applicable control.)*
- **WS1 — Guard false-positive fix** ✅ *shipped 3.3.0* — **deny-by-default**: keep the prior co-occurrence deny as the floor (no protection removed), allow back ONLY a provably-safe single read command (no chaining/substitution; leading verb in a strict write/exec-free read allowlist). Path basename net narrowed + `fpn` normalized (`./`, trailing `/`, `..` fixpoint) — fixes a real `.vscode/settings.json` report and the `./`/`../name` escapes. Dual corpus in `conformance/agent-autonomy.sh` locks both directions; **four adversarial security reviews** (the first attempt's allow-by-default shipped 5 weaknesses → reverted → re-architected). Residuals (compound-mention denied; `sort -o`/`xxd -r`/`perl -pi` floor gaps; var/`eval` indirection) are documented + agent-boundary-gate-backstopped.
- **WS2 — Risk-tiered solo review lane** ✅ *shipped 3.4.0* — default = recorded `reviewer`-subagent review + recorded human ratification (the logged independent review); high-risk (control-plane/security/data/prod/irreversible) = + a human structured self-review with specific acknowledgments tied to findings (anti-theater). Compliance-honest *compensating controls* → enforced two-human SoD via a single `enforce_admins: true` flip (zero rework of recorded evidence) when a teammate joins. Shipped: `templates/REVIEW-RECORD-TEMPLATE.md` · `docs/operations/review-lane.md` · §12 net-zero pointer · audit-evidence SoD row · `conformance/review-lane.sh`. Honest ceiling: the high-risk self-review is process discipline, not a fail-closed gate (agent-boundary CI gate backstops control-plane diffs).
- **WS3 — Progressive-disclosure front door** ✅ *shipped 3.5.0* — `START-HERE.md` gets a top "you do not need to read all of this" block: the first-5 core path (START-HERE · CLAUDE · DEVELOPMENT-PROCESS · profile · AGENTS) + a pull-not-push trigger map (regulated → enterprise/; live → operations/; data → continuity/; artifact → templates/); matching note in `ONBOARDING.md`; enterprise index framed pull-not-push. Nothing deleted, no gate disabled. Locked by `conformance/onboarding-complete.sh`.
- **WS4 — Persona routing** ✅ *shipped 3.7.0* — `ONBOARDING.md` "Which role are you?" table routes the non-engineer personas (PO/Designer/QA/Security Owner/DevOps) to their entry artifacts + the authoritative §2, with a "rigor is carried, not waived" note (gates are persona-blind). Interactive `incept` now prompts operator-fluency (non-coercive empty-enter). *Security Owner in the §13 ratification table was verified already present (added in WS2) — no edit.* Residual: the `incept` :118-119 fluency membership test loose-matches a multi-token string (pre-existing, non-exploitable, reviewer-confirmed) → tracked for a control-plane validation-hardening ticket.

### H2 — Containment reference (Tier 2) — ship the boundary the guard only documents
- **H2a — Reference platform boundary** ✅ *shipped 3.8.0* — *a verify-before-build pass found the egress-allowlist config was already shipped (11b: concrete default-deny + allowlist NetworkPolicy in `docs/operations/egress-control.md` + `conformance/egress-policy.sh`), and "no-egress devcontainer" was a category error for a dev inner-loop (it needs egress for package installs).* Reframed to **sandbox-FS devcontainer + egress-allowlist pairing** and closed the two real gaps: (1) the `typescript-node` profile now **dogfoods** the read-only-FS pattern it documents — a host-isolated `agent` service in `compose.yaml` (`read_only` + `tmpfs` + work-tree-only mount + `cap_drop: [ALL]` + `no-new-privileges` + `network_mode: none`, opt-in behind a compose profile so the verified app path is untouched) + a matching `.devcontainer/devcontainer.sandbox.json`; (2) concrete **AWS/GCP/Azure OIDC-federation** snippets in `containment.md` §2 (was prose-only). FS-sandbox and egress kept honestly separate; enforcement stays platform-owned. Design: `docs/superpowers/specs/2026-06-18-h2a-containment-reference-design.md`.
- **H2b — Non-Claude inline guard** ✅ *shipped 3.9.0* — `kit-guard install-shims` installs PATH-shims (curated single-invocation dangerous binaries) that call `kit-guard cmd` before `exec`, giving Codex/Cursor adopters automatic inline coverage with no per-command integration. **Honesty correction:** the prior `runtime-guards.md` claim of "full-matrix coverage" was wrong — a shim sees one binary's argv *post-shell-parse*, so it is **single-invocation only** (blind to pipes/redirects/chaining, absolute-path calls, interpreters); the full-string path stays `kit-guard cmd`, and shim integrity needs an agent-unwritable mount (H2a) — `install-shims` warns otherwise. `conformance/shim-coverage.sh` proves deny+allow+passthrough+no-recursion. Folded the H2a `.sandbox.json` heuristic tidy. Design: `docs/superpowers/specs/2026-06-18-h2b-install-shims-design.md`.

### D1 — Continuous drift detection — make semantic-drift detection continuous, not heroic
Structural drift is caught continuously (badge/links/coverage-meta); SEMANTIC drift (doc-claims-vs-code, staleness) is caught only by the periodic go/no-go.
- **D1a — claims-registry meta-check** ✅ *shipped 3.10.0* — `conformance/claims.tsv` (control-plane: claim → verifier) + `conformance/claims-registry.sh` generalise `badge-version.sh` from one claim to N: every registered headline claim's verifier must pass, and a `REQUIRED_IDS` coverage assertion catches a **silently-dropped** claim (weakening a verifier stays gated by ratification). Seed = 6 delegations to existing checks + the new `conformance/claim-gate-counts.sh` (the "seven required" / "five conditional" gate words == the §14 enumeration). CI-wired. Design: `docs/superpowers/specs/2026-06-18-d1a-claims-registry-design.md`.
- **D1b — scheduled go/no-go-lite** ✅ *shipped 3.11.0* — `.github/workflows/drift-watch.yml` (weekly Mon 06:00 UTC + `workflow_dispatch`, `contents: read`) re-runs the deterministic payload (`verify.sh --require` + `claims-registry.sh` + `check-links.sh`) so drift surfaces with **no commits** (toolchain rot, quiet-period staleness). **Fail-the-run** surfacing (D4-consistent: drift IS a real failure; deterministic payload → no flaky false-alarm). `conformance/drift-watch-wired.sh` (CI-wired + registered as a claim) keeps the drift-watcher from itself rotting. Networked/semantic-heavy checks (external-link, staleness) deferred — the workflow is their home. Feeds `sparkwright doctor` (P3). **D1 complete.**
  - *Tracked follow-up (from D1b review):* ✅ **closed in H4b (3.15.0)** — `action-pinning.sh` now globs the kit's own `.github/workflows/*.yml` in addition to the profile reference, so ci.yml + drift-watch.yml pins are mechanically enforced.

### H3 — Agentic-risk hardening (Tier 3)
- **H3a — Secret-in-context** ✅ *shipped 3.12.0* — the guard now denies the agent *reading* secret material into its context (the read half of exfil → model/logs/PR), symmetric with the existing secret-*write* deny. Two paths: `guard_check_command` denies shell content-reads (`cat`/`grep`/`diff`/`source`/… of `.env*`/`.pem`/`.key`/`id_rsa`/`secrets/`), and a new `guard_check_read` + the `Read` matcher deny the **Read tool** on secrets — but NOT control-plane reads (the read-deny ⊊ write-deny asymmetry). Deny-by-default + `KIT_GUARD_SELFEDIT` escape. Two security reviews of the scratch (fork-bomb-grade rigor): caught `source .env`/`. .env`/`.env*` glob/`.env.staging` slips → fixed + regression-locked in `agent-autonomy.sh` (216 cases). Honest ceilings: interpreter channel, exotic shell suffix, jq-absent. Design: `docs/superpowers/specs/2026-06-18-h3a-secret-in-context-design.md`.
  - *Tracked follow-up:* ✅ **closed in 3.17.0** — `guard_check_path` secret-WRITE deny broadened to mirror `guard_check_read`: now enumerates the same `.env.<suffix>` set (`.env*`, `.pem`, `.key`, `id_rsa`, `secrets/`) with the matched template allow-list (`.env.example`/`.sample`/`.template`/`.dist`); regression cases added to `conformance/agent-autonomy.sh`. The control-plane read⊊write asymmetry (H3a) is unchanged.
- **H3b — Cost/token governance** ✅ *shipped 3.13.0* — the honest two-layer model (mirroring containment): the kit ships the **contract** (a per-run budget + stop discipline — TCC `Budget` field + RUNBOOK `Cost governance:` line; monitored by `agent-trace.sh`'s `cost`/`tokens`) and references the **platform cap** (Anthropic API usage limits / harness budget) as the real enforcement, because a PreToolUse guard cannot see token counts. `conformance/cost-governance-ready.sh` (single-aspect three-state, registered as a claim) verifies the posture is declared+attested, never that spend was capped. `docs/operations/cost-governance.md`. Design: `docs/superpowers/specs/2026-06-18-h3b-cost-governance-design.md`.
  - *Tracked follow-up (from H3b review):* ✅ **closed in 3.17.0** — `conformance/cost-governance-ready.sh` applicability now triggers on a metered-LLM/AI feature (an `evals/` dir, a filled AI System Card, or `Agentic: yes`) in addition to a deploy surface, so a metered LLM CLI with no Dockerfile no longer silently escapes to N/A; new selftest fixtures cover the LLM-feature trigger. The N/A escape remains valid for genuinely-unmetered projects.
- **H3c — Long-session drift self-check** ✅ *shipped 3.14.0 (practice; docs-only)* — the agent's in-loop re-check during a long build, *before* any gate sees the drift; the institutionalized form of the verify-before-build pass that repeatedly caught this kit's own roadmap/docs over-promising. Five axes (intent/scope · plan · standards · **claim-vs-reality** · context-loss), checkpoint-triggered. **Scope = Hybrid (settled):** ships the **practice** now — `docs/operations/drift-self-check.md` + pointers from `agentic-ops.md` (agent-side complement to observation) and `review-lane.md` (run it before requesting review); **deliberately no conformance gate** (gating "did you self-check?" = unverifiable self-attestation = ceremony — the honest enforcement is the practice + the real downstream gates). Honest ceiling: a guard sees command strings not intent, so semantic drift is non-mechanical. Design: `docs/superpowers/specs/2026-06-18-h3c-drift-self-check-design.md`. **H3 complete (H3a + H3b + H3c).**
  - *Folded into P3:* the **mechanizable** axes (re-run conformance · re-check claims-registry · git ground-truth) are what `sparkwright doctor` will automate; the semantic axes (intent/scope, overclaim judgment) stay agent/human judgment (see P3 below).

### H4 — Coverage gaps (Tier 4)
- **H4b — Kit's own tool supply chain** ✅ *shipped 3.15.0* — **trust root first** (settled: H4b before H4a — a compromised tool can make any gate, incl. future GitLab ones, falsely pass). Corrects the stale "all unpinned/unverified" claim with the verified state + closes the real gaps: (1) `conformance/action-pinning.sh` now enforces SHA-pins across the kit's **own** `.github/workflows/*.yml`, not just the profile reference (closes the D1b follow-up); (2) the GitLab profile's syft/cosign/gitleaks installs are now **checksum-verified** against each pinned version's published `*_checksums.txt` (`sha256sum -c`, fail-closed via `test -s`) — replacing the prior `curl … | sh`-from-`main` (syft) and no-checksum binary downloads; (3) `conformance/supply-chain-verify.sh` (new, registered claim) locks the hardening (fails on a returning `curl|sh` or a missing verify); (4) `docs/operations/tool-supply-chain.md` documents the trust model + honest ceiling. **Honest ceiling:** verifies pin + checksum-against-published-manifest, NOT upstream-release integrity (next tier = keyless cosign verify of the checksums `.sig`) nor the runner base image (platform-owned — `jq`/`gh`/`shellcheck` root in `ubuntu-latest`, out of scope). Design: `docs/superpowers/specs/2026-06-18-h4b-tool-supply-chain-design.md`.
- **H4a — GitLab governance parity** ✅ *shipped 3.16.0* — **honest-scope, doc-first.** Branch-protection, ratification, and DORA enforcement are GitHub-first; GitLab already has full CI-gate parity (`ci-gates.sh` reads GitLab job keys) + the platform-neutral guard, and degrades honestly elsewhere (`branch-protection.sh` → UNVERIFIED off GitHub; `dora.sh` → "unavailable"). What shipped: (1) `docs/operations/gitlab-adoption.md` — an adopter guide consolidating the manual wiring for three governance areas: branch-protection settings (UI walkthrough), control-plane-ratification keystone (MR-approval as the non-author-review equivalent, wired via `CODEOWNERS` + a protected approval rule), and DORA (pipeline-level instrumentation pattern); (2) the GitLab profile note now points to `gitlab-adoption.md`; (3) `conformance/gitlab-adoption-complete.sh` — a drift-guard that fails if the guide's three required sections disappear or if the profile's cross-link is removed; registered claim → 11 claims total. **Deliberate non-scope decisions (recorded):** the kit did NOT build a GitLab branch-protection API check (would need a live GitLab instance to verify — untestable = the over-claim trap; also reverses the documented "out of scope, adopter-owned" decision in `ci-platforms.md`) nor an MR-approval ratification gate (same untestable-without-live-instance reason). Both remain adopter-owned. **H4 complete (H4a + H4b).** Next → P3.

### P3 — Growth & verification
- ✅ **Named harness adapters (codex/cursor/gemini)** *(shipped 3.21.0)* — first-class floor adapters beyond `generic`, each conformance-locked (`conformance/named-adapters.sh`; claim → 15) and protecting its own control surface (`.cursor/rules/`, `GEMINI.md`/`.gemini/`) in the agent-boundary union. **Floor-only by honest necessity** — no third-party harness has a native inline guard; the floor (`pre-push` + CI `agent-boundary`) is caller-agnostic. The live "this harness blocked a bad command" demo is **adopter-verified** (the README self-verify checklist), folded into the pre-release living-reference-project validation — the kit does not maintainer-claim a third-party harness's live behavior.
- ✅ **`sparkwright doctor`** *(shipped 3.18.0)* — adopter-facing POSTURE command that composes `conformance/verify.sh` (GATING) + `conformance/claims-registry.sh` (GATING) + an inline git ground-truth dimension (ADVISORY/WARN-only) into one sweep; `--full` adds `dora.sh` + `agent-scorecard.sh` as an INFORMATIONAL metrics section (never affects exit code). **Absorbs the mechanizable half of H3c's drift self-check** (axes D claim-integrity + E git ground-truth are now automated; the semantic axes stay agent/human). Protected in the guard's control-plane named-set. Registered `doctor` claim → **12 claims total**. `conformance/doctor-wired.sh` regression-locks the selftest in CI.
- ✅ **Close the operate loop — Slice 1 (incident → postmortem → backlog)** *(shipped 3.19.0)* — `sparkwright postmortem new` generates a postmortem stub from incident metadata (scaffolds `templates/POSTMORTEM-TEMPLATE.md`); `sparkwright postmortem to-backlog` parses action items into backlog stubs (human-ratified; no incident auto-detection / no live-tracker auto-creation — the kit's never-actuate principle). `docs/operations/operate-loop.md`. `conformance/operate-loop-wired.sh` regression-locks the selftest in CI; registered `operate-loop` claim → **13 claims total**.
- ✅ **Close the operate loop — Slice 2 (DORA + scorecard → tier RECOMMENDATION)** *(shipped 3.20.0)* — `sparkwright tier-advice` renders the **decision view** (read-only) over `agent-scorecard.sh`'s already-emitted directives: which agents have a pending autonomy-tier recommendation + the human-ratified apply path (auto-downgrade = fail-safe; raise → Security owner §13). DORA appears as labeled delivery-health context (never a tier input). Emits, never actuates; no classification logic added, no tier change auto-actuated. Wired into the `sparkwright` dispatcher and guard control-plane named-set; locked by `conformance/tier-advice-wired.sh` (claims registry → 14).
- ✅ **Broaden the front door** *(shipped 3.22.0)* — the front door (WS3 progressive disclosure + WS4 persona routing) and the discovery layer are already shipped. This slice adds the **typescript-node CLI archetype reference** (`profiles/typescript-node/scaffold-cli/`, conformance-locked via `profile-completeness.sh`) and corrects the compose-copy over-claim in `docs/STACK-SELECTION.md`. `--archetype` flag, batch/worker scaffolds, and per-stack CLI scaffolds are P4.
- ✅ **Stand up a living reference adopter project (pre-release) — the real dogfood** *(RUN 2026-06-20 — findings ranked in "Pre-release dogfood findings" below)* — *the framing that matters before release.* The **Feedback-Triage Service** (`~/Development/feedback-triage`, a synthetic typescript-node app) was inception'd and taken through the full loop cold-adopter style (separate fresh session, product-only opening prompt) — Inception + 2 increments (persistence, AI triage with a live adversarial eval gating CI) + deploy/operate + a real backup-restore drill; 11 human-ratified PRs; **every CI gate executed on real GitHub Actions at least once.** Verdict: the *process spine is strong and validated by building* (builder≠reviewer caught a real defect every round; eval-driven dev held up 27/28, 0 injection breaches; DR was the strongest-designed layer), while the *shipped artifacts* share **one root cause — validated piecemeal, never executed end-to-end against a real filled-from-template adopter project.** Findings are ranked below; the adopter project's `KIT-FIELD-NOTES.md` / `KIT-REVIEW-SUMMARY.md` / `KIT-ASSESSMENT.md` are the source of record. A **dogfooding audit (2026-06-19)** had already framed why this was the real closer — self-use is **lopsided by kind, not just by coverage**:
  - **Adopter *governance* half — dogfooded heavily + live.** The guard, `conformance/` suite, `control-plane-ratification` gate, §14 CI gates, and builder≠reviewer are **identical whether the product is an app or the kit**, and they run on our own repo every PR. So this half is genuinely battle-tested as adopter machinery — not merely "developing-IT" tooling.
  - **Adopter *application-lifecycle* half — never lived.** The parts that only activate when the product is a real running application are unexercised: a **live stack profile** (the profiles are validated as *artifacts*, never lived as a *build*); **feature TDD/coverage/the test pyramid/evals** on business logic; the **operate loop** on real data (`traces/` absent, `scorecards/` empty, `postmortems/` empty — `trace→scorecard→tier-advice`+`postmortem` tested on fixtures only); **inception + the artifact templates as *lived documents*** (we drive dev with the superpowers toolchain, so `templates/SPEC`/`TASK-CONTEXT-CONTRACT`/`RUNBOOK`/`EVAL-PLAN` pass presence-checks but aren't lived); a declared **per-run budget** (cost-gov H3b not self-applied); `doctor` **as the release gate** (only `--selftest`'d in CI). *(DR/observability/deployment/monitoring are correctly **N/A** for a static markdown+shell repo — not gaps.)*
  - **The structural truth:** the kit's **own repo can never fully dogfood the kit**, because the kit isn't an application — there is nothing to deploy, no data service, no feature backlog in a real stack. The `typescript-node` "verified path" is a conformance **fixture** (proves the reference satisfies its contract in isolation), **not** a living project that goes through the lifecycle. So today's honest claim is only *"the builder-facing pieces are present and self-consistent,"* **not** *"the adopter experience is proven."*
  - **The real closer (bigger than a slice):** stand up a **living reference adopter project** — a real app, **inception'd** in a chosen stack, taken through the full loop (inception → feature slices with TDD/coverage/evals → operate loop with real traces + a real `postmortem` → declared budget → `doctor` as the gate → deploy/monitor where applicable). Folds in the model-fit finding to watch for: **MP-3b presumes persistent *named* agents with trailing history; our dev pattern is ephemeral subagents-per-task** (no stable `agent.id` to baseline) → running the operate loop on our own transcripts emits little until there's a real project with stable agents. This is why self-dogfooding belongs in the pre-release pass, not mid-backlog.

### P4 — Polish (Low)
- **Operability / meta-docs** — RUNBOOK incident-response section; meta-doc staleness sweep (dates, counts).
- **Misc** — designer handoff guidance (axe/Lighthouse); ts-node AI-security pointer + eval scaffold; rename documentation-only CI step labels.
- **Multi-stack scaffold / IDE friction** — the kit ships reference scaffolds for every stack in one tree (`profiles/<stack>/scaffold/`), so a language IDE extension auto-discovers *all* of them, not just the adopter's chosen stack — e.g. VS Code C# Dev Kit finds `profiles/dotnet/scaffold/app.sln` and fails a NuGet restore (cosmetic; nothing in-repo broken). Add a one-line note in each profile / `profiles/_TEMPLATE.md` that scaffolds are inert templates and IDE tooling should be scoped to the chosen stack via *local* prefs (e.g. `dotnet.defaultSolution: disable`), never a committed `.vscode/` (an adopter who picks that stack wants the tooling live).

---

## Pre-release dogfood findings (Feedback-Triage stress test, 2026-06-20)

Synthesized from the living-reference adopter project (above). Raw source of record: the adopter repo's `KIT-FIELD-NOTES.md` (≈35 timestamped entries), `KIT-REVIEW-SUMMARY.md` (prioritized defect report H1–H6/M1–M6/A1–A3/W1–W8), and `KIT-ASSESSMENT.md` (qualitative, who-is-this-for). **All G-findings below were re-verified live on `main` @ `bbfffb2` (v3.22.0)** — the cold session's fixes lived in the adopter repo, not the kit. Each becomes its own loop slice (spec → plan → subagent-build → independent review → release); control-plane slices via the AMBER scratch+apply mechanic. **Scope decision (ratified): do all G + all S, then the strategic backlog.**

**One root cause (G1–G7, H-series):** the kit's shipped *artifacts* were validated piecemeal but **never executed end-to-end against a real, filled-from-template adopter project** — every defect surfaced the first time a layer actually *ran* (CI on first PR, conformance on first sweep, Dockerfile on first build, provenance on first merge). The *process* layer, which **was** exercised end-to-end, held up.

### Real kit gaps — G-series (ranked)

| # | Gap | Severity | Surface | Fix |
|---|-----|----------|---------|-----|
| **G1 ✅ *3.23.0*** | Reference `profiles/typescript-node/ci.yml` (+ kit `.github/workflows/ci.yml`) carries 4 defects that only appear when the workflow runs on GHA: (a) `hashFiles()` in a job-level `if:` (`image-provenance`, :159) → **invalid GHA document**, 0s startup failure; (b) `${{ github.base_ref }}` spliced into a `run:` (:210) → fails the kit's **own Semgrep SAST gate** (shell-injection); (c) `gitleaks-action` commit-range mode → "Invalid revision range" on multi-commit PRs; (d) secret-scan job lacks `pull-requests: read` for the action's PR API call. | **Critical** | control-plane (`ci.yml` ×2) | Fix all 4; switch secret-scan to range-independent `gitleaks dir .` (stronger, event-independent, drops the PR-perm need); checksum-pin the gitleaks binary. |
| **G2 ✅ *3.31.0*** | **No execution-test of the reference CI/Dockerfile**, and no `actionlint` in conformance — the meta-fix. The reference `ci.yml` is asserted by *gate-id presence*, never run as a GHA document; the `Dockerfile` is never `docker build`-ed. | **Critical** | control-plane (conformance + CI) | **Shipped:** `actionlint` already landed in G1; G2 adds `.github/workflows/golden-path.yml` — a path-filtered/weekly/dispatch job that scaffolds a temp ts-node project and RUNS the reference artifacts end-to-end (npm pipeline + `docker build` + `/healthz` liveness), staging the COPY-&-ADAPT Dockerfile into the context. Retroactively executes G4/G6/G10/G12. `golden-path-wired.sh` regression-lock (claim 19). **Scoped (documented residuals):** no `act` (runs commands, not the GHA engine — actionlint validates the YAML); no registry-push provenance (logic gated by G7). Proven locally end-to-end (docker build → boot → `{"status":"ok"}`). |
| **G3 ✅ *3.24.0*** | Conformance **silently skips** governance gates on the kit's *own* template output: `privacy-ready.sh` greps `data classification:` but `PROJECT-CLAUDE-TEMPLATE.md` emits `**Data classification** (§privacy): Confidential` (colon after the parenthetical) → a **Confidential project bypasses its DPIA gate**; `agentops-ready.sh` anchors `^…agentic: yes` but the template says backticked mid-line `set \`Agentic: yes\``; `eval-ready.sh`/`responsible-ai-ready.sh` only probe hardcoded paths (miss `docs/sign-offs/`, the location the threat-model template itself recommends). Silent-skip (exit 0/N/A) is worse than a loud fail. | **Critical (safety)** | control-plane (conformance) | Make detection regexes match the templates' own output; search `docs/sign-offs/`/a manifest; **add a conformance test that stamps the real PROJECT-CLAUDE template + artifacts and runs the suite against it** (so the kit's format can't drift from its own regexes). Also: make `claims-registry`/`claim-gate-counts`/`ci-selftest-coverage` tolerant of (or not shipped to) adopters who must edit the vendored CI. |
| **G4 ✅ *3.25.0*** | Reference `profiles/typescript-node/Dockerfile` doesn't run for **Prisma — the profile's own default ORM**: (a) `npm run build` (tsc) with no `npx prisma generate` → tsc fails (no `@prisma/client` types) and runtime has no client; (b) no `binaryTargets` guidance → image builds then **crashes on first DB query** (query engine not built for the distroless runtime platform). | **High** | agent-editable (profiles) | Add `prisma generate` to the builder stage + a `binaryTargets` note in `schema.prisma`; `docker build` + boot/smoke the reference in kit CI (see G2). |
| **G5 ✅ *3.25.0*** *(via scaffold `.gitignore` re-include; universal incept-strip optional follow-up)* | Kit root `.gitignore` ignores `/src/` and `/test/` (lines 22–23, "stray dogfooding output"), but `incept` copies the starter scaffold **into** `src/`+`test/` → app source is **silently untracked**; `git add -A` skips it, breaking "scaffold is CI-green on first push." | **High** | control-plane (incept) | `incept` strips `/src/`+`/test/` from the project `.gitignore` (or the shipped `.gitignore` doesn't carry them). |
| **G6 ✅ *3.26.0*** | The ts-node default archetype is a DB-backed service shipping a Postgres `compose.yaml`, but the reference `ci.yml` provisions **no database** → the integration tests the profile's §6 prescribes have nowhere to run. | **High** | control-plane (`ci.yml`) | Ship a commented-out opt-in `services: postgres` + `prisma generate` + `migrate deploy` block; document that `prisma generate` must precede type-check/build. |
| **G7 ✅ *3.26.0*** | SLSA provenance jobs (`provenance` + `image-provenance`, push-to-main only) are **structurally un-passable on a user-owned private repo** — the most common solo-adopter config — so `main` goes **red on first merge** with no in-repo explanation. Correct otherwise (public/org repo flips both green at full strength). | **High** | control-plane (`ci.yml`) | Gate the provenance jobs on repo visibility/ownership the way image jobs gate on `has_dockerfile`: full strength on public/org, **skip-with-logged-reason** (N/A-with-reason) on user-owned private; document the precondition. |
| **G8 ⏸️ *deferred — needs per-segment*** | Control-plane guard: the co-occurrence heuristic matches raw substrings **anywhere in a command, incl. quoted prose** (blocks `pip install` + a path, a `gh pr create --body` describing a CI fix, even a field-note mentioning `.env`/`sed`); `KIT_GUARD_SELFEDIT=1` is **unreachable** when set inline on a Bash command (hook runs in a separate pre-spawned process); shell-deny vs Write-tool-deny **asymmetry** (a `python open(…,'w')` bypasses what the Write tool blocks for governance `.md`). | **Med (delicate — security)** | control-plane / security (guard-core) | **DEFERRED (2026-06-20, documented).** A narrow fix (anchor `install` + `git checkout -b` exclusion) was built; the mandatory dual security-review of the scratch caught **confirmed bypasses** before transfer (whole-string `-b` negative suppresses a chained real `git restore <cp>`; anchoring `install` drops wrapper/pipe coverage). **Root cause: the guard greps substrings over the whole command — every safe narrow fix collides with whole-string-negative bypasses, so the real fix is per-segment parsing** (split on `;`/`&&`/`||`/`|`; judge each segment's leading verb vs the paths in *that* segment) — a deliberate larger slice with its own security pass. Deferred because the FP fails *safe* (over-deny), the fix risks the *unsafe* direction, and the real backstop is the PR `gate-agent-boundary`. FP + `!`/Read-tool workaround documented in `docs/operations/runtime-guards.md`. Salvageable design: deny `git restore` OR `git checkout --`-pathspec OR non-branch-create checkout (verified zero-regression). Also still includes selfedit-sentinel (must be agent-uncreatable) + asymmetry (safe atop per-segment). |
| **G9 ✅ *3.26.0*** | `semgrep --config auto --error` is **non-deterministic** for a *required* gate — pulls rules from the registry at runtime, so pass/fail varies by environment (locally flagged the kit's own `guard-core.sh`; CI didn't). | **Med** | control-plane (`ci.yml`) | Pin a ruleset for the SAST gate; `.semgrepignore` the guard. |
| **G10 ✅ *3.25.0*** | The ts-node scaffold pins `vitest` / `@vitest/coverage-v8` `^2.1.0`, which carry known **high+critical** advisories (dev-only; the gate passes via `--omit=dev`, but a fresh project starts day-one with `npm audit` findings). **= the pending kit-wide vitest-4.x bump.** | **Med** | agent-editable (profiles) | Bump scaffolds to vitest 4.x across both archetypes (service + CLI); keep scaffold dev-deps current. |
| **G11 ✅ *3.29.0*** | `incept` copies `.github/CODEOWNERS` verbatim with non-existent `@your-org/*` teams → enabling `require_code_owner_reviews` would block all merges. | **Med** | control-plane (incept) | **Shipped WARN-only:** incept warns (stderr, exit-0) when the copied CODEOWNERS still contains `@your-org/*`, pointing to `review-lane.md`. Substitution rejected as dishonest — `--intent-owner` is a human name, not a GitHub `@org/team`, so there is no valid value to substitute; placeholders left as fill-me-in markers. |
| **G12 ✅ *3.25.0*** | No runnable `smoke.sh` / `dr-drill.sh` for the service archetype — only prose (a one-line `curl` in the RUNBOOK template; `backup-restore-drill.md`). Both authored + validated in the adopter project (candidate kit artifacts). | **Med** | agent-editable (profiles) | Ship `scripts/smoke.sh` (healthz + core-flow) + a Postgres `dr-drill.sh` (`pg_dump`/`pg_restore` into an isolated scratch DB, fail-closed on source/protected names, null-safe all-column checksum) for the service archetype. |
| **G13 ✅ *3.28.0*** | Over-promise wording — profile `ci.yml`/scaffold README claim "verified green with the real npm pipeline" (true for npm *steps*, the file was never run as a GHA workflow → root of G1); `inception-done.sh` parses the harness field too literally (a trailing period `claude-code.` → looked for `adapters/claude-code.` and failed). | **Low** | mixed (docs + conformance) | Soften to "npm steps verified" until G2 executes the workflow; strip trailing punctuation/whitespace in the harness parse. |
| **G14 ✅ *3.27.0*** | No "secrets for AI features" playbook + the "agent cannot run its own live eval" boundary is undocumented. The guard correctly blocks the agent loading a live key into context → "see the eval pass before wiring CI" is a **human/CI step, not an agent step** for AI features. Secret-handling was the single biggest operational friction (paste artifacts, key-rotation-without-secret-update, IDE-attached `.env` line). | **Low** | docs | Add a short secrets-for-AI playbook (set CI secrets from a file, never hand-paste; don't rotate mid-loop; don't select the key in-editor); state the agent-can't-run-live-eval boundary in the eval-driven-dev guidance. |

> **Product noise — explicitly NOT kit gaps:** project-specific CI database wiring (correctly *not* synced to the profile; only the *recommendation* to ship an opt-in block is the gap = G6); per-project conformance gaps (missing BIA, RUNBOOK posture lines — these prove detection *works*, closable per-project); secret-paste 401s / IDE-attached `.env` line (operational, motivates G14 only); the initial `git push -u origin main` needing a human (one-line doc note at most).

### Strategic adoption epics — S-series (from `KIT-ASSESSMENT.md`)

The assessment's central structural finding: **the kit's approachability gradient is inverted** — it is *least* usable for the audience that most needs guardrails (the newcomer / "vibe coder") and *most* valuable for the audience that needs them least (the seasoned enterprise team). Four quadrants (Newcomer × {Prototype, Enterprise}, Seasoned × {Prototype, Enterprise}); today's sweet spot is **Seasoned/Enterprise** only. Persona verdicts: *Newcomer/prototype → not yet* (needs a prototype mode + env preflight); *Newcomer/enterprise → only with a human owner* (silent-skip gaps = false safety, G3); *Seasoned/prototype → usable but heavy* (wants a mode dial); *Seasoned/enterprise → recommended* (fix CI+Dockerfile day one, then a genuinely strong agentic SDLC). The highest-leverage usability work is the **left column + top row**, not the (already strong) bottom-right.

- **S1 ✅ *3.34.0*** — Process-weight mode. `incept --mode prototype|team|enterprise` (default `team`; recorded as `Process mode:` in project CLAUDE.md §3) curates **scaffolding + surfacing** to the declared mode: prototype/team write a trigger-named conditional-obligation checklist (`docs/conditional-obligations.md`, never a premature N/A); enterprise proactively stamps the governance apparatus (`docs/governance/*`). **P2-tension resolved provably:** enforcement is **mode-blind** — `conformance/mode-enforcement-blind.sh` (claim `mode-blind`, claims → 22) asserts no gate across the enforcement surface (conformance · gating scripts · workflows · pre-push) reads the stamped mode, so a mode can **never weaken an applicable control**. **Honest scope: S1 adds NO enforcement/safety** — the conditional-applicability ratchet already auto-activates each control on its detected trigger; the mode is pure friction-removal + surfacing (what P2 prescribes). The `--explain`/why-teaching layer is **S4**.
- **S2 ✅ *3.32.0*** — Adopter-environment preflight (extended `preflight.sh`, not a competitor). `preflight.sh` now auto-surfaces, **only inside a `github.com` repo**, three advisory checks: (a) **repo class** → warns a user-owned-private repo that the SLSA provenance gate will silently skip (G7); (b) **workflow validity** → reuses `conformance/actionlint-valid.sh` so an invalid GHA document is flagged *here* (G1/G2); (c) a **standing** `@your-org` CODEOWNERS re-check (G11 incept warns only once). **WARN-only** (never changes preflight's exit code); honest `skip — <reason>` when `gh`/`actionlint`/network is absent; the advisory count is now displayed. Honest scope = **reuse + surface + detect-actual-environment**, not new validators. Locked by `conformance/adopter-preflight-wired.sh` (claim `adopter-preflight`, claims → 20).
- **S3 ✅ *3.33.0*** — Adopter-clean obtain / prune. **Git-native** `.gitattributes export-ignore` (6 **link-safe** maintainer-only paths: `docs/ROADMAP-KIT.md`, `drift-watch.yml`/`golden-path.yml`, `scripts/fixtures/`, + the gitignored scratch) + `scripts/adopter-export.sh <dest> [--profile <stack>]` (`git archive --worktree-attributes HEAD` → clean tree, auto-excludes gitignored scratch/`node_modules`; `--profile` prunes the 9 unused stacks; fail-loud on archive failure, no-clobber dest, validated `--profile`) + honest README (**392 → 242 files** for typescript-node, the export-ignore-only-affects-`git archive` caveat). Resolved the S2-surfaced CODEOWNERS sub-finding (kit's own `.github/CODEOWNERS` → `@SeaBrad72`). Locked by `conformance/adopter-export-wired.sh` (claim `adopter-export`, claims → **21**; asserts the export-ignore set + **static link-safety** + the prune). Scoped honestly: the headline win is the 9 dead profiles + the 2 lingering workflows; three maintainer docs (`MAINTAINING`/`CHANGELOG`/`WALKTHROUGH`) stay (markdown-link targets).
- **S4 ✅ *3.35.0*** — The `explain` why-layer (the last S-series epic). The kit *enforced* but rarely *taught* — a newcomer learned *that* they need a threat model, not *why*. New **`docs/why-gates.md`** single-sources the rationale (12 teachable topics — the S1 conditional-obligations set + high-value §14 floor gates; each = trigger · 1–3 sentence *why* · enforcing check · *Read more*), readable on any harness. New **`scripts/explain.sh`** + `sparkwright explain <topic>` / `--list` is a read-only, **mode-blind** reader over it; the S1 checklist + the `privacy`/`eval`/`dr` headers cross-link to it. **Honest scope: S4 adds NO enforcement** — the why is synthesized from the existing standards. Drift-locked by `conformance/explain-wired.sh` (claim `explain`, claims → **23**): no orphan rationale (repo-path `Enforced by:` must exist; `..`-escapes rejected) + **teaching-completeness** (every S1-checklist enforcer *and* every floor topic is taught), so a gate can never be added without its *why* nor a documented topic silently vanish. (Newcomer-audience lever, pairs with S1.)
- **✅ *Resolved in S3 (3.33.0)* — the kit's OWN `.github/CODEOWNERS`** *(surfaced live by the S2 standing CODEOWNERS check on `main`, 2026-06-21)* was the unadapted `@your-org` reference template; S3 adapted it to the kit's real owner `@SeaBrad72` (the `profiles/<stack>/CODEOWNERS` templates keep `@your-org` for adopters, who get theirs via `incept`).
- **⏳ Deferred (own slice) — adopter-clean `conformance/` carve** *(scoped out of S3, 3.33.0)* — the kit-self `conformance/` suite (~75 files; several check the *kit itself* — `claims-registry`, `doc-budget`, `badge-version`, `claim-gate-counts`, the `*-wired` locks) is the largest remaining adopter baggage, but pruning it safely needs `verify.sh`/`doctor` to **gracefully skip a missing check** — which carries a real safety hazard (an adopter who deletes a *real* gate would get a silent skip, not a failure) — plus a 75-file adopter-facing-vs-kit-self triage. Its own slice: design the skip-missing tolerance (with a security-review of the FN risk) + the triage, or relocate kit-self checks under a marked `conformance/kit-internal/` glob.

**Sequencing (default, re-prioritize at a retro):** G1 (+ actionlint, the G2 red-first piece) → G2 (golden-path execution test) → G3 (silent-skip safety) → G4 → G5 → G6 → G7 → G8 (security, scratch-reviewed) → G9 → G10 (vitest bump) → G11 → G12 → G13 → G14 → then S2/S3 (defect-adjacent strategic) → S1 (mode-dial epic) → S4. The kit-wide **vitest-4.x bump** is G10; the **refactor pass** (scope TBD) is sequenced after the G-series, recon at that point.

---

## Strategic arc — "Ironclad agentic SDLC" (the E-series) — *captured + ranked 2026-06-21 (gap-assessment: `docs/superpowers/specs/2026-06-21-e-series-gap-assessment.md`)*

**Assessment verdict (the unifying finding).** Every dimension shows the same shape: **strong contract · honest conformance that verifies DECLARATION not BEHAVIOUR · thin (often prose-only) reference.** The kit proves *"the discipline is declared"* well and *"the thing actually works"* rarely — the dogfood's "validated piecemeal, never executed end-to-end" generalized to every surface. **The E-series spine: move conformance attestation → behaviour (golden-path execution tests per dimension) and reference prose → runnable. G2 proved the template; the E-series applies it everywhere.** **Ranking:** Tier 1 = **E3** (orchestration — widest gap, the differentiator) · **E4 + DAST/image-vuln blind spots** (the safety floor under E3) · **E2** (feature flags are PRESCRIBED-ONLY, owner-flagged, smallest slice). Tier 2 = **E1 / E5 / E6** references (proven-but-thin). Tier 3 blind spots (fold in): managed-secrets **rotation/break-glass**, **multi-team fleet** (→E3), **on-call/SLA** reference (→E5), data governance/lineage, runtime/DAST security. Compliance crosswalks PROVEN at L2 (L3 evidence-automation deferred).

**Vision (owner-affirmed).** Anyone — vibe-coder · designer · product owner · solo dev · senior/principal/architect, individual or team — picks up the kit and ships **ironclad product + infrastructure**, with agents that **specialize, orchestrate, and scale safely** behind **bulletproof guardrails** that make agentic freedom safe and efficient. The kit must deliver a **full enterprise stack** that scales appropriately, with a deliberate bias to **front-loading rigor** (scale · compliance · governance) — because in the agentic world it is cheaper and faster to build the long-term needs in up front than to retrofit them.

**Governing principle — close the prescribed → proven/provided gap.** The kit today *prescribes* (in the standards) more than it *enforces* (in conformance) or *demonstrates/ships* (in the reference). "Prescribe where appropriate" stands — but **proven/provided is the bar** for this arc. Each epic ships as a contract → reference → conformance vertical so the discipline is executable, not described.

**The epics (unranked — the gap-assessment orders them):**
- **E1 — Full test battery, *proven*.** Make the pyramid explicit (each layer → the gate that enforces it → the scaffold file that demonstrates it); demonstrate **integration · contract · e2e · security · a11y** in the typescript-node reference (today: unit+coverage, smoke, evals are gated; the rest are prescribed-only). Regression stays "your CI suite *is* your regression suite."
- **E2 — Continuous delivery / release engineering, *proven*.** **Feature flags/toggles** reference + conformance; **decouple deploy from release**; kill-switch / rollback drill (like the DR drill); trunk-based + always-deployable-`main`. (Today: progressive-delivery + DORA + environments exist; flags are mentioned-only.)
- **E3 — Agentic specialization & orchestration, first-class *(owner: emphatic YES)*.** A conformance-held **roster of specialist agents** (test-author · migration-author · threat-modeler · perf-optimizer · doc-writer · reviewer · security-reviewer …) **plus** reusable **orchestration patterns** (fan-out · pipeline · parallel-review panels · adversarial-verify · handoff contracts) shipped as **adopter-facing capability**, not just maintainer process. The headline differentiator.
- **E4 — Bulletproof agent containment, *proven*.** The real boundary beyond the guard speed-bump: **egress allowlist · sandboxed FS · scoped/short-lived tokens · cost/token circuit-breaker**, provided as reference, not just declared (`containment-ready.sh` is a declared posture today). The enabler of safe parallel-agent scale. *(hardening-watch Tier 2/3.)* **Decomposition (sized by the E3 design §10 containment contract):** **E4a** boot+probe the sandbox → FS-scope/egress/caps PROVEN *(✅ shipped — see below)* · **E4a′** scoped-tokens/prod-SoD honest static check (cloud-IAM owned, not bootable) · **E4b** image-vuln CVE-scan gate · **E4c** DAST/runtime-security reference · **E4d** cost-ceiling/runaway kill-switch · **E4e** R2's deferred bot-identity ratification gate (author≠approver) *(✅ shipped — see below)* · **E4f** G8 per-segment guard. Each its own slice; E4a first (thinnest proven).
- **E5 — Production feedback / observability, *proven*.** OpenTelemetry + error-tracking reference wiring the **trace → scorecard → tier-advice** operate loop on **live telemetry** (today: DORA-from-GitHub + declared observability). "Production teaches the next iteration" needs a wired sensor.
- **E6 — AI-native quality depth, *proven*.** Eval-harness depth + **prompt-injection / red-team** reference + **LLM observability** (token · cost · quality tracing) — the kit's signature "production-grade AI app" surface, currently the least demonstrated.
- **E7 — Best-practice & supply-chain adherence (cross-cutter; likely the owner's unnamed #3/#5).** **SAST/DAST** in CI · SBOM/**SLSA** completion · **secrets-at-scale** (Vault/KMS) reference · dependency hygiene · enterprise-scale (multi-team · RBAC · compliance-at-scale) *proven*.

**Cross-cutting lens — front-load rigor + progressive disclosure.** Every E-epic's reference makes the enterprise-grade path the **default**, surfaced progressively (the S1 mode-dial + S4 explain are the template) so a novice isn't crushed and an architect isn't constrained. This lens is a review criterion on every epic, not a separate epic.

**Process:** ranked by a strategic **gap-assessment** (the prescribed-vs-proven audit, in flight) → each epic gets its own brainstorm → spec → plan → build; **E3 (orchestration) brainstormed first**, informed by the cold-adopter **retest** (which doubles as empirical evidence for E1/E2/E5). See [[pre-release-dogfood]] for the retest, [[hardening-watch]] for E4's threat themes.

---

## RETEST-2 fix-forward (second-stack stress test — Python/FastAPI, kit v3.36.1, 2026-06-22)

Second cold-adopter run (`SeaBrad72/feedback-triage`, Python/FastAPI **generated** profile) on v3.36.1. **Verdict: the kit materially hardened and demonstrably learns** — **zero** kit-caused GHA failures (vs v1's 4), builder≠reviewer caught a real defect on all 3 increments (stack-agnostic), conditional gates fired on real triggers, and v1's H6 is now regression-locked (`provenance-precondition.sh`). The two original export bugs were already closed by **S3a (#148) + S3b (#149)**. The newly-surfaced weaknesses are mostly **stack-independent** — invisible on v1 only because TS ships the *complete* reference profile (the generator path + the solo-merge boundary were never exercised). Source of record: the adopter repo's `KIT-FIELD-NOTES.md` (31 entries) + `docs/retros/2026-06-22-kit-validation-second-stack-run.md` + the kit-side `docs/superpowers/dogfood/FIELD-NOTES.md`.

**Ratified plan (2026-06-22): fix-forward FIRST (R1–R4), THEN build the E-series.** First E-build = **E2** (owner-ratified); **brainstorm E3 now** (it sizes E4); build order **E2 → E4 → E3 → E1/E5/E6**.

**E2 ✅ SHIPPED — v3.41.0 (2026-06-22).** Feature flags: kill-switch floor, PROVEN. Zero-dep default-OFF flag registry (`src/flags.ts`) + flagged `GET /greeting` + flag-aware `smoke.sh` + a `golden-path` two-boot flip drill that proves the kill-switch end-to-end (OFF→kill-switch / ON→feature; also closes the "smoke wired into CI" gap-item) + two-part conformance (`feature-flags-wired` behaviour lock · `feature-flags-ready` adopter conditional; claims 23→25). Expand-contract migration · canary/blue-green drill · managed-provider (OpenFeature/Unleash) · live runtime flips are documented as next steps in `docs/operations/feature-flags.md`, not built (deferred to E1/E5/later).

**E4a ✅ SHIPPED — v3.42.0 (2026-06-22).** Containment-audit: the agent sandbox is **PROVEN** to contain (first E4 build). Moves three of the four platform controls from attestation to behaviour — `scripts/containment-audit.sh` boots the shipped `agent` sandbox (`docker compose --profile agent run`) and probes **FS-scope** (write outside `/work` fails, inside succeeds), **egress** (`network_mode: none` blocks outbound), **caps** (`cap_drop: [ALL]` blocks a CAP-gated op), each negative probe paired with a positive control so a dead container can't pass vacuously; fail-closed under CI; adopter-runnable; added to the guard's control-plane set. Live proof = the `containment-audit` job in `golden-path.yml` (real docker boot+probe on PR + main, the G2 bar). Locked by `conformance/containment-audit-wired.sh` (claim `containment-audit`, claims 25→26; carved from the adopter export like `feature-flags-wired`). Scoped-tokens/prod-SoD stay honest attestation (cloud-IAM owned) → **E4a′**. Design retired in T3c (rationale in this entry + CHANGELOG). **CI-surfaced reference finding (flagged follow-up, not in E4a's containment scope):** the `agent` sandbox runs the `builder` stage as root, and `cap_drop:[ALL]` strips DAC_OVERRIDE, so it **cannot write the `/work` bind mount on Linux** (only on Docker-Desktop/Mac, which virtualizes ownership) — a real reference-usability gap (candidate fix: a `user:`-mapping option or a documented uid requirement). The audit's positive control was corrected accordingly (`/work` mounted+readable; write-capability proven via tmpfs). **Next: the `/work`-writability reference fix · E4a′ / E4b… → then E3 (orchestration).**

**E4b ✅ SHIPPED — v3.43.0 (2026-06-23).** Image-vuln gate: a Trivy CVE scan that actually gates (closes the "SBOM enumerates, nothing gates" blind spot). `gate-image-vuln` (SHA-pinned `aquasecurity/trivy-action`, **fixable CRITICAL/HIGH**: `severity CRITICAL,HIGH · ignore-unfixed · vuln-type os,library · exit-code 1`) added to all **7 Dockerfile profiles**; `container-supply-chain.sh` requires it profile-wide; a golden-path `image-vuln` job **proves** it — reference image PASS + a pinned known-vulnerable fixture blocked **by actual findings** (a `jq` vuln-count, not just a non-zero exit → non-vacuous RED, closing the unpullable-fixture false-green hole security review caught). `golden-path-wired.sh` locks it (no new claim; stays 26). Honest boundary: gates *actionable* (fixable) risk; unfixed CVEs enumerated by the SBOM. Design retired in T3c (rationale in this entry + CHANGELOG).

**E4a′ ✅ SHIPPED — v3.44.0 (2026-06-23).** Token-scope static gate — completes the 4-platform-controls coverage (E4a proved FS/egress/caps behaviourally; this adds a static structural gate for scoped-tokens). New `conformance/token-scope.sh` (claim `token-scope`, claims 26→27) over `profiles/*/ci.yml` + `.github/workflows/*.yml`: (1) the **top-level** `permissions:` block grants no `id-token: write`/`write-all` (OIDC must be **job-scoped**), (2) **no long-lived AWS/Azure/GCP static keys** (curated; GITHUB_TOKEN + role ARNs allowed). Mirrors `provenance-precondition.sh`; no export-carve. Honest boundary: static-structural on shipped workflows, NOT a behavioural cloud-IAM proof; prod-cred SoD stays RUNBOOK-attested (`containment-ready.sh` unchanged). ★ security-review-of-scratch (7th consecutive slice) caught a CRITICAL evasion pre-merge — the block-walk reset on a blank line/comment/tab (a workflow-wide grant after one = false PASS); fixed + locked with fixtures. Design retired in T3c (rationale in this entry + CHANGELOG).

**E4c ✅ SHIPPED — v3.45.0 (2026-06-23).** DAST / runtime-security — closes the last named blind spot. Hardened the reference app (`server.ts`) with 4 zero-dep security headers on every response (`X-Content-Type-Options: nosniff` · `X-Frame-Options: DENY` · `Content-Security-Policy: default-src 'none'` · `Referrer-Policy: no-referrer`); the `golden-path` job asserts them on the **booted** container (deterministic, non-vacuous); `conformance/runtime-security.sh` (claim `runtime-security`, claims 27→28) static-locks the headers + the assertion (carved from the adopter export, mirrors `containment-audit`; the hardened server.ts ships). DAST itself = a **documented** OWASP ZAP baseline reference in `security-scanning.md` (opt-in, NOT a forced gate — avoids false universality). Honest boundary: proves the runtime-security floor on the trivial reference; full DAST against real surface is the adopter's; HSTS intentionally absent (kit terminates plain HTTP). ★ security-review-of-scratch = first clean scratch review of the arc (APPROVE; 2 Minor folds: check_wf all-4-headers anti-rot + apply.py NoReturn). Design retired in T3c (rationale in this entry + CHANGELOG).

**E4 agent-sandbox parity + /work-mount fix ✅ SHIPPED — v3.46.0 (2026-06-23).** Closes the E4a `/work` follow-up + a propagation gap (the hardened agent sandbox shipped only in ts-node — H2a never propagated). (1) Added the hardened `agent` sandbox to **all 7 container profiles'** compose.yaml (read_only, network_mode:none, cap_drop:[ALL], no-new-privileges, work-tree-only mount, opt-in); provided-for-all, proven-on-ts-node. (2) Fixed `/work` writability on Linux: the builder runs as root + cap_drop strips DAC_OVERRIDE → can't write a host-owned bind mount; the service now runs as `${HOST_UID:-1000}:${HOST_GID:-1000}` (`HOST_UID=$(id -u) HOST_GID=$(id -g) docker compose --profile agent run`), keeping every containment property (non-root = stronger). `containment-audit.sh` re-promotes the `/work` write probe INFO→gated POSITIVE (`POS fs-work: PASS`), proven live on Linux. No new claim (28). ★ security-review confirmed the user-mapping weakens NO containment negative (strictly stronger than root). Known limitation noted: the audit egress probe is node-dependent → ts-node-only (the 6 are reference configs, not booted). Design retired in T3c (rationale in this entry + CHANGELOG). **★ ALL 4 PLATFORM CONTROLS + DAST/image-vuln blind spots covered (E4a/E4b/E4a′/E4c) + the sandbox is now usable + present across all container profiles. Remaining E4: E4e (bot-identity gate) · E4f (G8 guard); E4d (cost/runaway) → E3 *(superseded — the M1 meta-control first run decoupled E4d from E3; see the reprioritized backlog above)*. Then E3 (orchestration). Surfaced follow-up: should the audit egress probe be generalized if 'prove all 7' is ever chosen.**

**E4e ✅ SHIPPED — v3.47.0 (2026-06-23).** The R2-deferred bot-identity ratification gate — makes **author ≠ approver** a *proven* control, not a convention, via **FLOOR + NATIVE** (the principle is forge-neutral; only the binding is forge-specific). FLOOR = `scripts/sod-check.sh`, a pure identity-set gate (PR/MR author vs approver-set vs commit-author-set; PASS only when a distinct ratifier exists; **fails closed under CI**; whole-line fixed-string comparison — no word-split/glob, any metachar is a literal identity), proven by `--selftest` (9 fixtures). NATIVE bindings: a copy-and-enable **GitHub** reference workflow (`docs/operations/sod-gate.github.yml`, injection-safe, not auto-run on the kit) + bot-identity-via-App pattern; **GitLab** native MR approval rules (*Prevent approval by author* / *…by users who added commits*). New `conformance/author-not-approver-wired.sh` (claim `author-not-approver`, claims 28→**29**) runs the FLOOR selftest + static-locks the bindings + actionlint-parses the workflow; **mode-blind**; `scripts/sod-check.sh` added to the guard's control-plane set. ★ **security-review-of-scratch earned its keep again** — caught 2 Important fail-opens (I-1 glob pathname-expansion of identities → env-dependent FALSE PASS, root-fixed via `grep -vxF`; I-2 literal `null` for unlinked-email commits, fixed via placeholder + honest-ceiling note); re-review APPROVE. Honest ceiling: kit proves the *logic*; server-side enforcement is the adopter's branch-protection/forge rules; `--admin` stays a human escape hatch (detective audit = deferred follow-up); SoD doesn't defend against collusion. Design retired in T3c (rationale in this entry + CHANGELOG). **Remaining E4: E4f (G8 per-segment guard — delicate, standalone); E4d (cost/runaway) → E3 *(superseded — the M1 meta-control first run decoupled E4d from E3; see the reprioritized backlog above)*. Then E3 (orchestration).**

**E4d ✅ SHIPPED — v3.49.0 (2026-06-26).** The cost/runaway kill-switch — the safety item E13 (FinOps) dissolved into, decoupled from E3. An executable, harness-neutral circuit-breaker `scripts/runaway-guard.sh` (`step`/`check`/`reset`) halts an orchestrated flow when cumulative tokens, step count, or agent-spawn count breach a control-plane ceiling `.kit/budget.conf`. It enforces a ceiling on *reported* usage at the orchestration seam; the platform LLM-API cap is the hard backstop. Conformance-locked `conformance/runaway-killswitch-wired.sh` (claim `runaway-killswitch`; wired into `verify.sh`/CI/drift-watch/doctor), with the ceiling guarded against agent self-raising (Write/Edit **and** shell-redirect deny in `guard-core.sh`, regression-locked in `agent-autonomy`). Honest ceiling: no hard spend cap, best-effort runtime tally (`.kit-run/`, gitignored), wall-clock out of scope. Built via AMBER apply.py + dual review (reviewer + security-reviewer APPROVE; security adversarially verified the fail-closed path into drift-watch/doctor — non-executable script → `verify --require` FAIL → aggregate nonzero). Design `docs/architecture/2026-06-25-e4d-runaway-killswitch-design.md`; ops `docs/operations/runaway-killswitch.md`. **Remaining E4: E4f (G8 per-segment guard, deferred). Next per the reprioritized backlog: `M2-ratification-hardening`.**

### Fix-forward — R-series (ranked; each its own loop slice, control-plane via the AMBER scratch+apply mechanic)

| # | Sev | Finding (field-note ID) | Fix | Disposition |
|---|-----|------|-----|-------------|
| **R1** | HIGH | `new-profile.sh` hard-codes `profiles/python/` (lines 86–88) → any non-TS profile generation **hard-fails on the FIRST Inception action** (B1, confirmed live); generated stub `ci.yml` also ships unpinned actions (B2), ungated provenance (B3), echo-TODO sbom/dep-scan (B4) — one root cause: the generator falls below the kit's own conformance bar | Conformant inline-companion stub + the **export-aware `generator-golden-path`** (B1 is export-only — only running the generator INSIDE an export catches it; the B-meta permanent kill) | ✅ **3.37.0** |
| **R2** | HIGH/process | Agent-boundary at merge is convention-not-gate in solo+agent: `gh pr merge --admin` bypasses the pre-push guard (server-side) AND the code-owner approval gate is **unsatisfiable** (author==sole owner; GitHub forbids self-approval) (A1+A2) | **Doc patch now** (retro opt b): document solo-track ratification = human `--admin` merge; guard covers only local git. **Deep gated fix (bot-identity authorship, opt a) → E4** | ✅ **3.38.0** (doc; +10 profile BRANCH-PROTECTION caveats) · gated fix → E4 |
| **R3** | MED-HIGH | `doctor.sh` reports **POSTURE: FAIL by construction** post-incept (`ci-selftest-cov`/`adopter-export` don't apply to an adopter) (C1); root `.gitignore:22–23` ships `/src/`+`/test/` → adopter source **silently un-committable** (C2, confirmed live) | Kit-self checks (`ci-selftest-cov`, `adopter-export`) self-skip N/A in adopter repos (fail-closed via the control-plane `golden-path.yml` marker); `adopter-export.sh` strips `/src/`+`/test/` from the exported `.gitignore` | ✅ **3.39.0** |
| **R4** | MED | `dr-ready.sh` over-claims "RECORDED" for an unrun drill (C3); stale `KIT_GUARD_SELFEDIT` inline guidance (C4); harness/kit spec-path collision — superpowers writes specs to kit-gitignored `docs/superpowers/specs/` (C7) | dr-ready requires a recorded date (4-digit year) + FAILs not-yet/pending; runtime-guards.md inline-selfedit caveat; `.gitignore`+brownfield document the spec-path collision | ✅ **3.40.0** |

**Folded elsewhere / deferred:** guard ergonomics — agent can't run the in-place DR drill (D1) + localhost `curl /healthz` blocked (D2) → **E4** with the deferred **G8 per-segment guard** (same root cause: whole-command substring grep); `gate-eval` secret-exposure default (C5) → **E6** (ship push-to-main / `environment:`-approval reference); Node-20 action pins (C6) → maintenance pin-refresh cadence.

### Testing (Stream 3, interleaved)
- **Generated-profile golden-path** (extend G2 to generate a profile + run its CI end-to-end) = the B-meta fix AND the permanent generator-class regression-kill → folds into R1.
- **Team-track dogfood** after R2/E4 — A1 is a *solo+agent* pathology; a two-identity / `enforce_admins:true` run validates the team path + the bot-identity gate.
- **Exit-interview harvest** (`docs/superpowers/dogfood/RETEST-2-exit-interview.md`) → pre-seed E8/E9/E10 with lived evidence.

### Meta-flag
The E-series was a large arc (originally E8–E14, now reorganized — item 5); the retest verdict is the **core is strong** (gaps are references/depth, not broken foundations). The **release-line decision is now made** (T3d, 2026-06-24: stay 3.x; gate 1.0 on feature-complete + E10 + a real external adopter — see the reprioritized backlog). E10 (the "is this too much?" capstone + one comprehensive validation + maintenance model) remains the close so the arc doesn't run open-ended.

---

**Last Updated:** 2026-06-27 (**skill-spine brick #1 — the kit's own design skill SHIPPED — v3.57.0** [`skills/design/SKILL.md`: kit-authored harness-neutral design/brainstorm methodology, invoked by the Orchestrator's Architect hat (read+follow), encoding the kit's own disciplines (design-intent · is-the-provable-thing-meaningful · proven-not-prescribed slice-selection · agents-vs-skills · honest-ceiling · right-weight) — conformance greps the markers so a generic copy fails; FLOOR-only-first (invoke-by-read; formal skills adapter dimension = brick #2); right-weighted (claim `skill-spine`, extends `orchestrator-loop-wired.sh`); `skills/` guard-immutable. First brick toward FULL replacement of superpowers (zero dependency; E10 acceptance). Architect = a HAT not a seat (agents-vs-skills); roles track continues in parallel]. Prior: (**E3b conflict-safe parallel writes SHIPPED — v3.56.0** [§10 item 6: the orchestration loop detects overlapping changed-file sets across parallel engineer branches + refuses integration fail-closed before any corrupting merge, trusted-layer `kit.conflict` span; proactive detect-by-inspection replaces implicit detect-by-failure; claim `conflict-safe-integration`, right-weighted (no new gate — extends `orchestrator-loop-wired.sh`); chosen over FS-isolation whose provable case was hollow]. Prior: (**E3-escalation human-in-the-loop seam SHIPPED — v3.55.0** [guard-breach raises a plain-language, role-addressed escalation record + pauses; resume only on a human-ratified verdict, fail-closed; B-ready schema for the deferred tier-checkpoint (Option B); `scripts/escalate.sh` + `conformance/escalation-wired.sh`; trusted-layer `kit.escalated`/`kit.verdict`/`kit.ratifier` anti-spoof; claim `escalation-seam`; proven by the deterministic golden-path — fail-closed pause + raise-ceiling resume]. Prior: (**release-tag guarded-manual + opt-in SHIPPED — v3.54.0** [course-correction on v3.53.0: removed the live auto-tag binding (it imposed a release model on adopters); the kit now tags via the coherence-guarded FLOOR helper run manually after merge; auto-tag-on-merge ships as opt-in GitHub/GitLab references]. Prior: (**auto-tag-on-merge SHIPPED — v3.53.0** [forge-neutral FLOOR `scripts/release-tag.sh` + live GitHub binding + GitLab reference + generic doc; tags `v<VERSION>` on the merge commit coherently/idempotently; removes the human from release tagging; lock `release-tag-on-merge`; existing release-coherence gate unchanged]. Prior: (**E3a — thin 4-seat orchestrator loop SHIPPED — v3.52.0** [Orchestrator + Engineer×N + Reviewer + Security; real `scripts/orchestrator-run.sh` replaces the E5-thin stand-in + wires `runaway-guard.sh step` (first live caller); FLOOR roster `agents/*.agent.md` + NATIVE `.claude/agents/` bindings + `orchestration` adapter dimension; deterministic fixture-engineer proof + golden-path `orchestrator-loop` job; claim `orchestrator-loop`. **Self-hosting ratified** — kit ships its own superpowers-equivalent, E10 = build a slice with only the kit's roster. Meta-control panel #5 = GO]. Prior: (**version-tag-coherent export-fix SHIPPED — v3.51.1** [adopter export N/A-skips `version-tag-coherent` on a non-git tree instead of UNVERIFIED→FAIL — restores green-on-clone, broken since v3.49.1; kit-self anchor on `docs/ROADMAP-KIT.md`, fail-closed, mirrors `feature-flags-wired.sh:49`; N/A-skip not carve]. Prior: (**E5-thin SHIPPED — v3.51.0** [OTel-shaped agent-ops sensor: an orchestrator stand-in emits an OTel span tree → thin adapter → the **unchanged** scorecard derives a real `denial_rate` from an emitted DENIED span, proven non-vacuously by a new golden-path `agentops-sensor` job + opt-in `otlp-export.sh` OTLP/JSON seam; honest ceiling = valid OTLP produced/POSTed, live backend is the adopter's, stand-in replaced by E3a; behaviour lock `agentops-sensor`, version finishing folded into apply.py]. **Both thin inputs are now in (E1-thin ✅ + E5-thin ✅) — NEXT = (3) E3a** (the thin orchestrator loop; replaces the stand-in body + wires `runaway-guard.sh step`). Prior: (**E1-thin SHIPPED — v3.50.0** [integration + e2e test layers in the ts-node reference (zero-dep, in-suite — the oracle E3 runs) + stack-neutral `test-layers-ready` gate; dual-reviewed APPROVE; ★ the release-coherence gate caught a 3rd skipped VERSION bump at tag-push — fixed forward]. **NEXT = (2) E5-thin** (one OTel-style trace + one real scorecard — the sensor E3 needs). Prior: **Meta-control panel #4 — GO-WITH-CONDITIONS** [`docs/architecture/2026-06-26-meta-control-4.md`]: ratified the feature-epic reorder (item 5 above) — **E3 is the spine, led by thin E1+E5 inputs, gated slice-by-slice**; overrode the "build E1/E5/E6 *through* E3" proposal as circular (F5 build-ahead-of-need); 0 blockers, 7 fix-forward Highs [§10 status table, wire runaway-guard in E3a, lead with thin oracle/sensor, escalation slice early, mode-dial T4, T2-team-live provisioning]; nothing cut. **NEXT = (0) governance-CODEOWNERS line → (1) E1-thin → (2) E5-thin → (3) E3.** Prior: **M2-ratification-hardening SHIPPED — v3.49.1** [release-coherence gate `version-tag-coherent` — VERSION↔git-tag, catches a skipped version bump at tag-push; + M2 future-pin/verdict-enum hardening + shared ver-helper. ★ The gate proved itself immediately — it caught a skipped VERSION bump on its own release.] Prior: **E4d runaway kill-switch SHIPPED — v3.49.0** [executable circuit-breaker at the orchestration seam; tokens/steps/agent-spawns ceilings; conformance-locked; ceiling agent-immutable]. Prior: **M2 cadenced-meta-control ENFORCEMENT COMPLETE** — S1 `3.48.14` export-hygiene · S2 `3.48.15` freshness gate · S3 `3.48.16` `.claude/agents/*` control-plane · S4 **first real panel run** [GO-WITH-CONDITIONS, `docs/architecture/2026-06-25-meta-control-3.md`]. The panel — the M machinery's first outing — found the breaker is **self-certifiable** (verdict log + marker not control-plane) and that the **adopter export is RED on `verify --require`** (pre-M2): **next, in order, is `M2-S5` ratification-integrity → adopter-RED fix → E4d**. Prior: **T2 real validation COMPLETE** — cold-adopter run of v3.48.0 + solo governance bundle proven [green-on-clone CONFIRMED, agent-review adversarial, self-merge blocked, FLOOR 9/9]; team logic-verified, live-deferred → `T2-team-live`; findings at `docs/architecture/2026-06-23-t2-real-validation-findings.md` routed to T3/T4. Prior this day: T1 honesty COMPLETE [F1 v3.47.1 · F3+F4 v3.47.2]; **M1 cadenced meta-control v3.48.0** — panel + first run [GO-WITH-CONDITIONS] resequenced the backlog above: T2-first, E4d decoupled, E3 not default-first, two CI-trust Blockers → T4; M2 enforcement next. Prior: RETEST-2 second-stack synthesis — R-series fix-forward ranked + ratified build order E2 → E4 → E3 → E1/E5/E6; brainstorm E3 now. Superseding: pre-release dogfood synthesis — G1–G14 + S1–S4 ranked; **13/14 G's closed** (G8 ⏸️ deferred, doc'd 3.30.0); **S-series COMPLETE** — **S2 ✅ 3.32.0** (adopter-env preflight) · **S3 ✅ 3.33.0** (adopter-clean obtain) · **S1 ✅ 3.34.0** (process-weight mode; mode-blind enforcement resolves the P2 tension) · **S4 ✅ 3.35.0** (the `explain` why-layer; teaching-complete, drift-locked); next = the refactor pass (scope TBD, recon when there) + the deferred adopter-clean conformance-carve)
