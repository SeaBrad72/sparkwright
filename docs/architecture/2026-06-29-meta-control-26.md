# Meta-control panel #26 — E5-ops-skill (the `operating` craft skill), v3.75.0

**Date:** 2026-06-29 · **Cadence:** per-slice light 5-lens (condition A5) · **Basis:** fresh-clone evidence + dual review.
**Slice:** E5-ops-skill — the kit's own `operating` craft skill (`skills/operating/SKILL.md`): blast-radius-aware, advisory-not-actuating operate-phase discipline, wired single-seat to the Orchestrator's Ops hat. **Second and final E5-ops sub-slice** (after E5-ops-query v3.74.0). Design: `docs/architecture/2026-06-29-e5-ops-skill-operating-design.md`.

## The 5 lenses

1. **Correctness / proof.** PASS. A pure FLOOR skill brick in the proven `continuous-discovery` shape. The conformance teeth are non-vacuous, proven by mutation: `conformance/orchestrator-loop-wired.sh` gains `check_operating_skill()` (6 kit-distinctive marker greps + Orchestrator-reference assertion) and **2 new load-bearing negative selftest cases** — case 26 (marker-teeth: dropping `autonomy tier` from the grep loop makes the case stop catching → exit 1) and case 27 (reference-teeth: Orchestrator def missing `skills/operating/SKILL.md` → FAIL). Fresh tagless clone: `orchestrator-loop-wired.sh --selftest` **27/27**, `verify --require` **36 control + 13 doc / 0 failed** with `skill-spine` PASS and the structural `check_keystone` GREEN (`skills/operating` indexed), shellcheck **109** clean, idempotent (2nd apply 0 changes). ★ The apply.py installs the 122 KB `orchestrator-loop-wired.sh` by base64-embedding the whole file behind a fail-closed guard; **both reviewers independently reproduced the apply and diffed it: ADDITIVE-ONLY** (every hunk an operating addition; no existing case/check/gate weakened) — the slice's primary risk, cleared.
2. **Scope / right-weight.** PASS. ~10 files (design estimated ~8; the `.claude/` orchestrator mirror + the `orchestration.md` brick-enumeration update are the delta — both necessary, both purely operating-related). **No new gate/claim/guard** — extends the existing `skill-spine` claim description (44 rows unchanged, no new id; no `verify.sh`/`ci.yml`/`adopter-export.sh` edit). Single-seat = Orchestrator Ops hat (Engineer def byte-untouched).
3. **Security / trust.** PASS (security seat, 5/5 clear). The core property holds: the SKILL.md is **unambiguously advisory-not-actuating** end-to-end — the agent surfaces + recommends, *the human commands the catastrophic action*, high-risk routes through `escalate.sh`; the rationalizations table explicitly closes the escape routes ("L1 → just act", "fix is obvious → apply it", "escalate.sh → auto-resolve"). No language authorizes autonomous catastrophic action. No secrets/PII; apply.py writes only declared paths (no traversal, no shell exec, anchor-count-checked, fail-closed); no new attack surface (craft prose + conformance wiring). The wholesale-embed introduces no unintended change (independently decoded + diffed).
4. **Honesty / ceiling.** PASS. The SKILL.md ceiling is genuinely honest: triage *quality* is **un-gateable** (advisory relationship; no CI check that an agent triaged a real alert well) — the structural proof is craft-provided + agents-vs-skills respected + escalation hand-off, not good judgement; the `escalate.sh` **ops-trigger is disclosed as banked/not-wired** (`escalate-ops-trigger-banked`), not implied; the skill *references* (does not duplicate) the operate substrate (`operate-loop.md`, `agentic-ops.md`, the §13 tier matrix). No green check over-claims.
5. **Process / coherence.** PASS. Executed via subagent-driven-development on the kit's OWN spine — **designed via the kit `design` skill, planned via the kit `plan` skill** (the self-host: an owner-caught process correction mid-session — superpowers `brainstorming` was wrongly invoked, then switched to the kit's own `design` brick). Builder ≠ reviewer (two independent seats: correctness APPROVE + security PASS, both reproducing the additive-diff). AMBER apply.py idempotent + clone-proven; version-finishing folded in. The one review Minor (file-count estimate) is documentation-grade.

## Verdict: **GO**

No conditions. The one Minor (design "~8 files" vs actual 10) is a documentation estimate, substance correct — no fold needed. Unlike the E5 behavioural slices there is **no live-delivery gating condition** (this is a craft-prose + conformance-wiring brick; the selftest + `check_keystone` are the full proof, and they're green on the clone). The slice is structurally sound and ready for AMBER apply.

## What this closes

Completes **E5-ops** (both sub-slices) and the **E5 epic's operate-loop back half**: the kit now both *proves* an operator can find a trace (E5-ops-query, v3.74.0) and *carries the craft* for acting on it safely (E5-ops-skill, v3.75.0). The skill spine reaches **10 content skills + the using-skills keystone** (design · plan · tdd · review · worktrees · verification · debugging · evals · continuous-discovery · **operating**).

## Routed to backlog
- **`escalate-ops-trigger-banked`** (banked at design): wire an `ops-irreversible` trigger + option set + `--selftest` into `scripts/escalate.sh` when a live consumer exists (the skill points at the seam; the ops trigger is the deferred mechanism).
- **★ NEXT (owner-requested, raise-first): the AI↔human handoff redesign** — its own governed slice (`ai-human-handoff-discussion` memory): explicit go/no-go decision gates (human ratifies GO; execution delegable post-GO), progressive gate rigor by environment (dev-PoC → prod), a formal AMBER/ship review handoff, relax agent-commit (post-explicit-GO, no unilateral commits), and make `control-plane-ratification` team/tier-aware for the solo interim. The `operating` craft (blast-radius → tier → advisory/escalate) is the worked template for it.
- Marker to advance: `3.74.0 GO` → **`3.75.0 GO`** (human-authored per M2-S5, with the pipe-table log row, folded into this slice's ship).
