# Meta-control panel #24 — E5-backend (live OTLP backend round-trip), v3.73.0

**Date:** 2026-06-29 · **Cadence:** per-slice light 5-lens (condition A5) · **Basis:** fresh-clone evidence + dual review (correctness opus + security).
**Slice:** E5-backend — a dedicated `otlp-backend` golden-path job boots the reference app, captures its emitted OTel spans, and POSTs them via the **existing** `scripts/otlp-export.sh` real path (no `--dry-run`) to a real **OpenTelemetry Collector**, asserting the exact emitted 32-hex `trace_id` + a decoded span appear in the collector's output. Fourth slice of epic E5; closes the E5-trace forward reference (emission+convertibility → **delivery**). A reference collector config ships (proven == provided). Design: `docs/architecture/2026-06-29-e5-backend-otlp-roundtrip-design.md`.

## The 5 lenses

1. **Correctness / proof.** PASS. The job is non-vacuous: step 5 greps the collector logs for `$EMITTED` (the app's random 32-hex `trace_id`, captured from `gp_spans.ndjson`) **AND** a decoded-span marker (`ResourceSpans|Span #|Trace ID`) — a dead/empty collector emits only startup noise → no random id → fails. Step 4 is the **real POST** (no `--dry-run`; `OTEL_EXPORTER_OTLP_ENDPOINT` set → `otlp-export.sh` takes the curl `/v1/traces` path). The receipt-assertion regex was de-risked **against real v0.116.1 collector output locally** before CI (Implementer A's docker round-trip confirmed `Trace ID`/`ResourceSpans`/`Span #` all appear). Lock `--selftest` 5/5, two load-bearing negatives (dry-run POST → FAIL, missing receiver → FAIL). Fresh clean-shell clone: `verify --require` **35 control / 13 doc / 0 failed** (+1 control), `otlp-backend` PASS, `version-tag-coherent` PASS; shellcheck **108** clean, actionlint 14 workflows valid.
2. **Scope / right-weight.** PASS. Lighter than the prior E5 slices — **no `server.ts`, no `otlp-export.sh` change** (the slice exercises the existing seam, retroactively validating it). A new dedicated job (mirrors the containment-audit/image-vuln/agentops sibling pattern), a new claim + lock, and a shipped reference config. No new gate type.
3. **Security / trust.** PASS (security seat, 0 findings). Localhost-only POST (no external telemetry egress); `OTEL_EXPORTER_OTLP_HEADERS` never set → the proven header-injection guard is untouched; no auth/secret/PII (span attrs are method/status/request-id/query-stripped-name; `trace_id` is random). **No two-matcher gap** — `conformance/*` prefix wildcard covers the new lock in BOTH guard matchers (`is_control_plane_path` + the shell-redirect regex). Image digest-pinned (`@sha256:da954d90…`); config mounted `:ro`; `EMITTED` validated `^[0-9a-f]{32}$` before use (`grep -F`). Carve correct (both loops + REQUIRED_IDS); the collector config is correctly NOT carved (adopter artifact).
4. **Honesty / ceiling.** PASS. Design states the floor: **delivery + ingest-acceptance** to a real collector on the reference app — long-term storage, trace query/retrieval, and a live vendor backend are the adopter's; the queryable-backend round-trip (Jaeger/Tempo + query API) is **banked for E5-ops**. Justification is harness-neutral (the kit's own zero-dep OTLP seam + Factor-14 telemetry).
5. **Process / coherence.** PASS. Full loop on the kit's own spine (design→plan→subagent-build→dual-review), builder ≠ reviewer (two seats, both clean PASS/APPROVE), AMBER `apply.py` idempotent + anchor-preflighted + clone-proven (11/11 files). ★ The build **caught a real plan defect**: the planned `check_wf` used `grep -qF` (substring), which is **vacuous** — the real-POST token is a substring of the `--dry-run` line; fixed in-build to an end-of-line–anchored `grep -qE` and re-proven load-bearing. ★ A clone-proof **verify discrepancy** (a subagent reporting `agent-autonomy`+`adopter-export` FAIL) was reconciled per-control to the `KIT_GUARD_SELFEDIT=1` artifact (env set → guard self-edit bypass flips the autonomy fixtures; `env -u` → 0 failed) — **not** a slice regression; the verify + reconciliation discipline earning its keep ([[subagent-guard-selfedit-artifact]]).

## Verdict: **GO** (one gating condition)

**Gating condition (not a code change):** the slice's whole value — *real receipt at a collector* — is exercised only when the PR's `otlp-backend` golden-path job runs. The clone-proof covered the static lock + `verify --require` + actionlint + a local docker round-trip, but the authoritative live proof is the CI job. **Confirm the PR's `otlp-backend` golden-path job is green before merge.**

## Routed to backlog
- **E5 next:** E5-ops (Ops/SRE seat — earns the demand-gated seat; the **queryable-backend** round-trip [Jaeger/Tempo + query API] folds in here, paired with the persona that queries traces).
- **Banked Minors (non-blocking):** (NB-otlp1) tighten the lock's image token from the substring `otel/opentelemetry-collector` to include `@sha256:` so a future un-pin to `:latest` is caught (currently consistent with the other heavy jobs' substring greps — not a regression). (NB-otlp2) `apply.py` is idempotent but not transactional — an abort mid-apply leaves earlier sections written; idempotent re-run recovers (handoff notes "re-run on abort"). (NB-otlp3, process) the human must NOT commit `apply.py` itself.
- **Marker to advance:** `3.72.0 GO` → **`3.73.0 GO`** (human-authored per M2-S5, with the pipe-table log row).
