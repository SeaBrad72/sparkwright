# Meta-control panel #25 — E5-ops-query (queryable-backend trace round-trip), v3.74.0

**Date:** 2026-06-29 · **Cadence:** per-slice light 5-lens (condition A5) · **Basis:** fresh-clone evidence + dual review.
**Slice:** E5-ops-query — the reference app's emitted spans, POSTed via the existing `scripts/otlp-export.sh` real path to a real **Jaeger** backend, are **stored and retrievable by `trace_id`** via Jaeger's query API (`GET /api/traces/{id}`). First of the two E5-ops sub-slices (the second, E5-ops-skill, is the `operating` craft). One gradient past E5-backend (receipt → queryable storage). Design: `docs/architecture/2026-06-29-e5-ops-query-trace-roundtrip-design.md`.

## The 5 lenses

1. **Correctness / proof.** PASS. The new dedicated `trace-query` golden-path job boots the reference app, captures its emitted span + the exact random 32-hex `trace_id` (validated `^[0-9a-f]{32}$`), boots a SHA-pinned `jaegertracing/all-in-one` (OTLP/HTTP ingest `:4318` + query API `:16686`), POSTs via the **existing** `otlp-export.sh` real path (no `--dry-run`), then polls `GET /api/traces/$EMITTED` and asserts `.data` non-empty AND `.data[0].traceID == $EMITTED` (case-insensitive) AND spans present. **Non-vacuous, verified live**: an empty store returns `.data == []` and fails all three arms; the app's random id cannot be fabricated by an empty backend. Build-time **local Jaeger round-trip de-risk** confirmed the jq assertion against the pinned image (`traceID` lowercase, `.data[0].spans` path, `spans:4`) — no adjustment needed. Lock `--selftest` 4/4 with load-bearing negatives (dry-run POST → FAIL; missing `/api/traces/` → FAIL; missing `trace-query: OK` → FAIL); mutation-check confirms `/api/traces/` is the retrieval tooth. Fresh tagless clone: `verify --require` **36 controls / 0 failed** (+1), shellcheck **109** clean, actionlint **14/14** workflows, idempotent 11/11.
2. **Scope / right-weight.** PASS. ~11 files mirroring the proven E5-backend/`runtime-security` pattern — **no new gate type** (extends golden-path + the claims registry). New claim `trace-query` inserted immediately after `otlp-backend` in all six satellite lists. **No new maintained backend config** (Jaeger all-in-one needs none) — portability rides the OTLP standard, not a second shipped config, avoiding the append-drift anti-pattern. `server.ts` and `otlp-export.sh` **byte-unchanged** — the slice queries the far end of the seam E5-trace/E5-backend already proved on the near end.
3. **Security / trust.** PASS (security seat, all six checks clean — 0 findings). Both Jaeger ports are localhost-only on the ephemeral CI runner (`trap cleanup EXIT`); `otlp-export.sh` byte-unchanged and `OTEL_EXPORTER_OTLP_HEADERS` unset (no secret in CI env); the shipped observability README documents auth headers as a placeholder (`x-api-key=...`) and explicitly states local Jaeger is a dev/CI reference, not a production backend — no open query endpoint shipped to adopters; image digest-pinned (`@sha256:836e9b69…`); captured spans are CI-ephemeral with the query string stripped from the span name (unchanged `server.ts`); `apply.py` writes only declared paths via root-anchored joins, no shell exec, anchor-count-checked text edits.
4. **Honesty / ceiling.** PASS. Design + CHANGELOG state the floor plainly: proves **storage + query round-trip** on the reference app via Jaeger in-memory — production durability/sampling/retention, a live vendor query API, log/metric query, and the operate **craft** (triage/decide-safely) are explicitly **out of scope** (the craft is the next slice, E5-ops-skill). Jaeger is the CI-proven local reference; any OTLP backend works via the standard (the vendor swap is the adopter's). No green check over-claims.
5. **Process / coherence.** PASS. Executed via `superpowers:subagent-driven-development` — fresh implementer per task group + task review + dual whole-branch review, builder ≠ reviewer (two independent seats: correctness APPROVE + security PASS). AMBER `apply.py` idempotent + anchor-preflighted + clone-proven; version-finishing folded into `apply.py` per the standing fix. The build's own discipline earned its keep twice: Implementer B caught and fixed a base64 chunk-width UTF-8 corruption (width-76 split a 3-byte em-dash → width-88); the local docker de-risk pre-validated the jq assertion before CI (the same discipline that caught the debug-exporter format on E5-backend). The plan had already internalized the E5-backend `grep -qF` lesson (anchored `grep -qE`) — not reintroduced.

## Verdict: **GO**

No conditions. Both review Minors are non-blocking and banked: (A) the Jaeger readiness probe uses `curl -sS` without `-f` — **identical to the proven `otlp-backend` sibling**, downstream-guarded by the POST (`set -e`) and the jq assertion; changing it here but not in the sibling would create asymmetry, so no fix. (B) a non-finding (decoded lock byte-identical to the reviewed artifact). The slice is structurally sound and ready for AMBER apply.

> **Live-delivery gating condition (as on E5-backend):** the static lock proves the round-trip is *wired*; the live proof is the `trace-query` golden-path job going green on the PR. Treat a green `trace-query` job as the ship gate, not a code change.

## Routed to backlog
- **Governance-log backfill DISCHARGED** here: the four missing rows for panels **#20 (3.69.0) · #21 (3.70.0) · #22 (3.71.0) · #23 (3.72.0)** are backfilled into `docs/governance/meta-control-log.md` (inserted before the #24 row). The marker (`.meta-control-last`) was already `3.73.0 GO` (= the #24 row), so marker↔log sync was intact throughout — the backfill is cosmetic history, not a marker correction. Closes the standing governance-log gap noted since panel #24.
- **E5-ops-skill = next** (slice 2 of 2): the `operating` craft skill — blast-radius-aware, advisory-not-actuating (the human commands the catastrophic action), Ops/SRE as the Orchestrator's **hat** (not a standing seat — the kit has no live system of its own; the seat is demand-gated on a live system + distinct prod authority). Gets its own brainstorm → design → plan. It *points at* the capability this slice proves.
- 2 Low banked (both no-action): Jaeger probe `-f` symmetry with `otlp-backend`; reviewer's non-finding B.
- Marker to advance: `3.73.0 GO` → **`3.74.0 GO`** (human-authored per M2-S5, with the pipe-table log row, folded into this slice's ship).
