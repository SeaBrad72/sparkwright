# adapters/__HARNESS__/

BYO adapter skeleton — generated by `sh scripts/new-adapter.sh __HARNESS__`. It ships **floor-only**: every dimension is at the Kit-enforced floor and `mcp-gate` is `n-a`, so `sh conformance/harness-adapter.sh adapters/__HARNESS__` exits 0 immediately.

## Adopting with __HARNESS__

*(Replace this section.)* Name the document `__HARNESS__` **actually auto-loads** and how the adopter wires it to `AGENTS.md`. If `__HARNESS__` has its own in-repo control surface (a rules directory, a settings file), say so here **and** declare it in `controlPlanePaths` — the kit's hardcoded floor does not cover a harness-specific namespace; the union does, and only while this directory is present.

## The honest ceiling for __HARNESS__

*(Replace this section.)* State plainly whether `__HARNESS__` has harness-native inline interception. If it does not, say so: it is stopped at `git push` and at the PR, **not** at the keystroke, and `sh scripts/kit-guard install-shims` covers its *shell* commands only. Close with **"You verify this for your harness; the kit does not claim it for you."**

## How to customize the manifest

1. **Set `controlPlanePaths`** — replace or extend the defaults with the actual control-plane paths for the `__HARNESS__` namespace (e.g. its config file, rules directory, or settings path).
2. **Set `contextFile`** to the document `__HARNESS__` actually auto-loads — verify it, do not assume it — and copy the entry contract (section 1) **verbatim** into that document.
3. **Upgrade a dimension to `"native"`** — if the harness supports inline pre-exec interception, native subagents, or an MCP gate, change the `"level"` to `"native"` and add a `"proof"` (`check` and/or `files`) so `harness-adapter.sh` can verify the claim rather than reject it as lying-native.
4. **Validate** — run `sh conformance/harness-adapter.sh adapters/__HARNESS__` after each change.

## Everything else is the shared floor

The dimension table, what "floor-only" means, floor vs. native, the shared-control-plane / union rule, the self-verify recipe, and the coverage-ceiling links live once in [`../README.md`](../README.md) — *"The shared floor"*. The full boundary contract, 7-dimension table, manifest schema, and conformance rules are in [`../../docs/operations/harness-adapters.md`](../../docs/operations/harness-adapters.md); the dimension set is **closed** — declare exactly the seven.
