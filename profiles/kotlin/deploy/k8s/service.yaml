# Reference k8s Service for the Kotlin / Spring Boot profile. COPY & ADAPT.
apiVersion: v1
kind: Service
metadata:
  name: kotlin-app
  labels:
    app: kotlin-app
spec:
  type: ClusterIP
  selector:
    app: kotlin-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
