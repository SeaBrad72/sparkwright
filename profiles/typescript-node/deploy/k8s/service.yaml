# Reference k8s Service for the TypeScript/Node profile. COPY & ADAPT.
apiVersion: v1
kind: Service
metadata:
  name: ts-node-app
  labels:
    app: ts-node-app
spec:
  type: ClusterIP
  selector:
    app: ts-node-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
