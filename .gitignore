# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~

# Python caches (the ml profile ships a reference evals/ harness)
__pycache__/
*.pyc

# Claude Code — personal/local settings (shared settings.json IS committed)
.claude/settings.local.json

# Logs
*.log

# Local tool cache (firecrawl web-search results) — never commit
.firecrawl/

# Internal build scaffolding — kept locally, NOT shipped in the public repo
# (specs/plans/reviews/roadmaps contain personal paths + pre-anonymization context)
docs/superpowers/
docs/ROADMAP-SLICE9.md
docs/ROADMAP-SLICE11.md

# Reference diagram (working input, not a shipped asset)
Product:Design + pre-story discovery loops.png
